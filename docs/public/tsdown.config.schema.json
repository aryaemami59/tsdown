{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$ref": "#/definitions/UserConfig",
  "definitions": {
    "UserConfig": {
      "type": "object",
      "properties": {
        "entry": {
          "$ref": "#/definitions/TsdownInputOption",
          "description": "Defaults to `'src/index.ts'` if it exists.\n\nSupports glob patterns with negation to exclude files:",
          "markdownDescription": "Defaults to `'src/index.ts'` if it exists.\n\nSupports glob patterns with negation to exclude files:",
          "default": { "index": "src/index.ts" }
        },
        "deps": {
          "$ref": "#/definitions/DepsConfig",
          "description": "Dependency handling options.",
          "markdownDescription": "Dependency handling options."
        },
        "external": {
          "anyOf": [
            { "type": "string" },
            { "type": "string", "format": "regex" },
            {
              "type": "array",
              "items": {
                "anyOf": [
                  { "type": "string" },
                  { "type": "string", "format": "regex" }
                ]
              }
            }
          ],
          "deprecated": "Use {@linkcode DepsConfig.neverBundle deps.neverBundle} instead."
        },
        "noExternal": {
          "$ref": "#/definitions/Arrayable%3C(string%7Cstring479082557)%3E",
          "deprecated": "Use {@linkcode DepsConfig.alwaysBundle deps.alwaysBundle} instead."
        },
        "inlineOnly": {
          "anyOf": [
            {
              "$ref": "#/definitions/Arrayable%3C(string%7Cstring479082557)%3E"
            },
            { "type": "boolean", "const": false }
          ],
          "deprecated": "Use {@linkcode DepsConfig.onlyBundle deps.onlyBundle} instead."
        },
        "skipNodeModulesBundle": {
          "type": "boolean",
          "deprecated": "Use {@linkcode DepsConfig.skipNodeModulesBundle deps.skipNodeModulesBundle} instead.",
          "default": false
        },
        "alias": {
          "type": "object",
          "additionalProperties": { "type": "string" }
        },
        "tsconfig": { "type": ["string", "boolean"], "default": true },
        "platform": {
          "type": "string",
          "enum": ["node", "neutral", "browser"],
          "description": "Specifies the target runtime platform for the build.\n\n- `node`: Node.js and compatible runtimes (e.g., Deno, Bun).   For CJS format, this is always set to `node` and cannot be changed.\n- `neutral`: A platform-agnostic target with no specific runtime assumptions.\n- `browser`: Web browsers.",
          "markdownDescription": "Specifies the target runtime platform for the build.\n\n- `node`: Node.js and compatible runtimes (e.g., Deno, Bun).\n  For CJS format, this is always set to `node` and cannot be changed.\n- `neutral`: A platform-agnostic target with no specific runtime assumptions.\n- `browser`: Web browsers.",
          "default": "node"
        },
        "target": {
          "anyOf": [
            { "$ref": "#/definitions/Arrayable%3Cstring%3E" },
            { "type": "boolean", "const": false }
          ],
          "description": "Specifies the compilation target environment(s).\n\nDetermines the JavaScript version or runtime(s) for which the code should be compiled. If not set, defaults to the value of `engines.node` in your project's `package.json`. If no `engines.node` field exists, no syntax transformations are applied.\n\nAccepts a single target (e.g., `'es2020'`, `'node18'`, `'baseline-widely-available'`), an array of targets, or `false` to disable all transformations.",
          "markdownDescription": "Specifies the compilation target environment(s).\n\nDetermines the JavaScript version or runtime(s) for which the code should be compiled.\nIf not set, defaults to the value of `engines.node` in your project's `package.json`.\nIf no `engines.node` field exists, no syntax transformations are applied.\n\nAccepts a single target (e.g., `'es2020'`, `'node18'`, `'baseline-widely-available'`), an array of targets, or `false` to disable all transformations."
        },
        "env": {
          "type": "object",
          "description": "Compile-time env variables, which can be accessed via `import.meta.env` or `process.env`.",
          "markdownDescription": "Compile-time env variables, which can be accessed via `import.meta.env` or `process.env`.",
          "default": {}
        },
        "envFile": {
          "type": "string",
          "description": "Path to env file providing compile-time env variables.",
          "markdownDescription": "Path to env file providing compile-time env variables."
        },
        "envPrefix": {
          "$ref": "#/definitions/Arrayable%3Cstring%3E",
          "description": "When loading env variables from `envFile`, only include variables with these prefixes.",
          "markdownDescription": "When loading env variables from `envFile`, only include variables with these prefixes.",
          "default": "TSDOWN_"
        },
        "define": {
          "type": "object",
          "additionalProperties": { "type": "string" },
          "description": "Replace global variables or [property accessors](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Property_accessors) with the provided values.\n\nSee Oxc's [`define` option](https://oxc.rs/docs/guide/usage/transformer/global-variable-replacement.html#define) for more details.",
          "markdownDescription": "Replace global variables or [property accessors](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Property_accessors) with the provided values.\n\nSee Oxc's [`define` option](https://oxc.rs/docs/guide/usage/transformer/global-variable-replacement.html#define) for more details."
        },
        "shims": { "type": "boolean", "default": false },
        "treeshake": {
          "anyOf": [
            { "type": "boolean" },
            {
              "type": "object",
              "properties": {
                "moduleSideEffects": {
                  "anyOf": [
                    { "type": "boolean" },
                    { "type": "array", "items": { "type": "string" } },
                    {
                      "type": "array",
                      "items": {
                        "type": "object",
                        "properties": {
                          "test": { "type": "string", "format": "regex" },
                          "external": { "type": "boolean" },
                          "sideEffects": { "type": "boolean" }
                        },
                        "required": ["sideEffects"],
                        "additionalProperties": false
                      }
                    },
                    { "type": "string", "const": "no-external" }
                  ],
                  "description": "**Values:**\n\n- **`true`**: All modules are assumed to have side effects and will be included in the bundle even if none of their exports are used.\n- **`false`**: No modules have side effects. This enables aggressive tree-shaking, removing any modules whose exports are not used.\n- **`string[]`**: Array of module IDs that have side effects. Only modules in this list will be preserved if unused; all others can be tree-shaken when their exports are unused.\n- **`'no-external'`**: Assumes no external modules have side effects while preserving the default behavior for local modules.\n- **`ModuleSideEffectsRule[]`**: Array of rules with `test`, `external`, and `sideEffects` properties for fine-grained control.\n- **`function`**: Function that receives `(id, external)` and returns whether the module has side effects.\n\n**Important:** Setting this to `false` or using an array/string assumes that your modules and their dependencies have no side effects other than their exports. Only use this if you're certain that removing unused modules won't break your application.\n\n> [!NOTE] > **Performance: Prefer `ModuleSideEffectsRule[]` over functions** > > When possible, use rule-based configuration instead of functions. Rules are processed entirely in Rust, while JavaScript functions require runtime calls between Rust and JavaScript, which can hurt CPU utilization during builds. > > **Functions should be a last resort**: Only use the function signature when your logic cannot be expressed with patterns or simple string matching. > > **Rule advantages**: `ModuleSideEffectsRule[]` provides better performance by avoiding Rust-JavaScript runtime calls, clearer intent, and easier maintenance.",
                  "markdownDescription": "**Values:**\n\n- **`true`**: All modules are assumed to have side effects and will be included in the bundle even if none of their exports are used.\n- **`false`**: No modules have side effects. This enables aggressive tree-shaking, removing any modules whose exports are not used.\n- **`string[]`**: Array of module IDs that have side effects. Only modules in this list will be preserved if unused; all others can be tree-shaken when their exports are unused.\n- **`'no-external'`**: Assumes no external modules have side effects while preserving the default behavior for local modules.\n- **`ModuleSideEffectsRule[]`**: Array of rules with `test`, `external`, and `sideEffects` properties for fine-grained control.\n- **`function`**: Function that receives `(id, external)` and returns whether the module has side effects.\n\n**Important:** Setting this to `false` or using an array/string assumes that your modules and their dependencies have no side effects other than their exports. Only use this if you're certain that removing unused modules won't break your application.\n\n> [!NOTE]\n> **Performance: Prefer `ModuleSideEffectsRule[]` over functions**\n>\n> When possible, use rule-based configuration instead of functions. Rules are processed entirely in Rust, while JavaScript functions require runtime calls between Rust and JavaScript, which can hurt CPU utilization during builds.\n>\n> **Functions should be a last resort**: Only use the function signature when your logic cannot be expressed with patterns or simple string matching.\n>\n> **Rule advantages**: `ModuleSideEffectsRule[]` provides better performance by avoiding Rust-JavaScript runtime calls, clearer intent, and easier maintenance.",
                  "default": true
                },
                "annotations": {
                  "type": "boolean",
                  "description": "Whether to respect `/*@__PURE__*\\/` annotations and other tree-shaking hints in the code.\n\nSee [related Oxc documentation](https://oxc.rs/docs/guide/usage/minifier/dead-code-elimination#pure-annotations) for more details.",
                  "markdownDescription": "Whether to respect `/*@__PURE__*\\/` annotations and other tree-shaking hints in the code.\n\nSee [related Oxc documentation](https://oxc.rs/docs/guide/usage/minifier/dead-code-elimination#pure-annotations) for more details.",
                  "default": true
                },
                "manualPureFunctions": {
                  "type": "array",
                  "items": { "type": "string" },
                  "description": "Array of function names that should be considered pure (no side effects) even if they can't be automatically detected as pure.\n\nSee [related Oxc documentation](https://oxc.rs/docs/guide/usage/minifier/dead-code-elimination#define-pure-functions) for more details.",
                  "markdownDescription": "Array of function names that should be considered pure (no side effects) even if they can't be automatically detected as pure.\n\nSee [related Oxc documentation](https://oxc.rs/docs/guide/usage/minifier/dead-code-elimination#define-pure-functions) for more details.",
                  "default": []
                },
                "unknownGlobalSideEffects": {
                  "type": "boolean",
                  "description": "Whether to assume that accessing unknown global properties might have side effects.\n\nSee [related Oxc documentation](https://oxc.rs/docs/guide/usage/minifier/dead-code-elimination#ignoring-global-variable-access-side-effects) for more details.",
                  "markdownDescription": "Whether to assume that accessing unknown global properties might have side effects.\n\nSee [related Oxc documentation](https://oxc.rs/docs/guide/usage/minifier/dead-code-elimination#ignoring-global-variable-access-side-effects) for more details.",
                  "default": true
                },
                "invalidImportSideEffects": {
                  "type": "boolean",
                  "description": "Whether to assume that invalid import statements might have side effects.\n\nSee [related Oxc documentation](https://oxc.rs/docs/guide/usage/minifier/dead-code-elimination#ignoring-invalid-import-statement-side-effects) for more details.",
                  "markdownDescription": "Whether to assume that invalid import statements might have side effects.\n\nSee [related Oxc documentation](https://oxc.rs/docs/guide/usage/minifier/dead-code-elimination#ignoring-invalid-import-statement-side-effects) for more details.",
                  "default": false
                },
                "commonjs": {
                  "type": "boolean",
                  "description": "Whether to enable tree-shaking for CommonJS modules. When `true`, unused exports from CommonJS modules can be eliminated from the bundle, similar to ES modules. When disabled, CommonJS modules will always be included in their entirety.\n\nThis option allows rolldown to analyze `exports.property` assignments in CommonJS modules and remove unused exports while preserving the module's side effects.",
                  "markdownDescription": "Whether to enable tree-shaking for CommonJS modules. When `true`, unused exports from CommonJS modules can be eliminated from the bundle, similar to ES modules. When disabled, CommonJS modules will always be included in their entirety.\n\nThis option allows rolldown to analyze `exports.property` assignments in CommonJS modules and remove unused exports while preserving the module's side effects.",
                  "default": true
                },
                "propertyReadSideEffects": {
                  "type": ["boolean", "string"],
                  "enum": [false, "always"],
                  "description": "Controls whether reading properties from objects is considered to have side effects.\n\nSet to `false` for more aggressive tree-shaking behavior.\n\nSee [related Oxc documentation](https://oxc.rs/docs/guide/usage/minifier/dead-code-elimination#ignoring-property-read-side-effects) for more details.",
                  "markdownDescription": "Controls whether reading properties from objects is considered to have side effects.\n\nSet to `false` for more aggressive tree-shaking behavior.\n\nSee [related Oxc documentation](https://oxc.rs/docs/guide/usage/minifier/dead-code-elimination#ignoring-property-read-side-effects) for more details.",
                  "default": "always"
                },
                "propertyWriteSideEffects": {
                  "type": ["boolean", "string"],
                  "enum": [false, "always"],
                  "description": "Controls whether writing properties to objects is considered to have side effects.\n\nSet to `false` for more aggressive behavior.",
                  "markdownDescription": "Controls whether writing properties to objects is considered to have side effects.\n\nSet to `false` for more aggressive behavior.",
                  "default": "always"
                }
              },
              "additionalProperties": false,
              "description": "When passing an object, you can fine-tune the tree-shaking behavior.",
              "markdownDescription": "When passing an object, you can fine-tune the tree-shaking behavior."
            }
          ],
          "description": "Configure tree shaking options.",
          "markdownDescription": "Configure tree shaking options.",
          "default": true
        },
        "loader": {
          "type": "object",
          "additionalProperties": {
            "type": "string",
            "enum": [
              "js",
              "jsx",
              "ts",
              "tsx",
              "json",
              "text",
              "base64",
              "dataurl",
              "binary",
              "empty",
              "css",
              "asset",
              "copy"
            ]
          },
          "description": "Sets how input files are processed. For example, use 'js' to treat files as JavaScript or 'base64' for images. Lets you import or require files like images or fonts.",
          "markdownDescription": "Sets how input files are processed.\nFor example, use 'js' to treat files as JavaScript or 'base64' for images.\nLets you import or require files like images or fonts."
        },
        "removeNodeProtocol": {
          "type": "boolean",
          "description": "Remove the `node:` prefix from built-in Node.js module imports. When enabled, rewrites import sources like `node:fs` to `fs`.",
          "markdownDescription": "Remove the `node:` prefix from built-in Node.js module imports.\nWhen enabled, rewrites import sources like `node:fs` to `fs`.",
          "default": false,
          "deprecated": "Use {@linkcode nodeProtocol nodeProtocol: 'strip'} instead."
        },
        "nodeProtocol": {
          "anyOf": [
            { "type": "string", "const": "strip" },
            { "type": "boolean" }
          ],
          "description": "Control whether built-in Node.js module imports use the `node:` protocol.\n\n- `true`: Add the `node:` prefix to built-in module imports.\n- `'strip'`: Remove the `node:` prefix from built-in module imports.\n- `false`: Do not transform built-in module imports.",
          "markdownDescription": "Control whether built-in Node.js module imports use the `node:` protocol.\n\n- `true`: Add the `node:` prefix to built-in module imports.\n- `'strip'`: Remove the `node:` prefix from built-in module imports.\n- `false`: Do not transform built-in module imports.",
          "default": false
        },
        "checks": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "legacyCjs": {
              "type": "boolean",
              "description": "If the config includes the `cjs` format and one of its target >= node 20.19.0 / 22.12.0, warn the user about the deprecation of CommonJS.",
              "markdownDescription": "If the config includes the `cjs` format and\none of its target >= node 20.19.0 / 22.12.0,\nwarn the user about the deprecation of CommonJS.",
              "default": true
            },
            "circularDependency": {
              "type": "boolean",
              "description": "Whether to emit warnings when detecting circular dependency.\n\nCircular dependencies lead to a bigger bundle size and sometimes cause execution order issues and are better to avoid.",
              "markdownDescription": "Whether to emit warnings when detecting circular dependency.\n\nCircular dependencies lead to a bigger bundle size and sometimes cause execution order issues and are better to avoid.",
              "default": false
            },
            "eval": {
              "type": "boolean",
              "description": "Whether to emit warnings when detecting uses of direct `eval`s.\n\nSee [Avoiding Direct `eval` in Troubleshooting page](https://rolldown.rs/guide/troubleshooting#avoiding-direct-eval) for more details.",
              "markdownDescription": "Whether to emit warnings when detecting uses of direct `eval`s.\n\nSee [Avoiding Direct `eval` in Troubleshooting page](https://rolldown.rs/guide/troubleshooting#avoiding-direct-eval) for more details.",
              "default": true
            },
            "missingGlobalName": {
              "type": "boolean",
              "description": "Whether to emit warnings when the `output.globals` option is missing when needed.\n\nSee [`output.globals`](https://rolldown.rs/reference/OutputOptions.globals).",
              "markdownDescription": "Whether to emit warnings when the `output.globals` option is missing when needed.\n\nSee [`output.globals`](https://rolldown.rs/reference/OutputOptions.globals).",
              "default": true
            },
            "missingNameOptionForIifeExport": {
              "type": "boolean",
              "description": "Whether to emit warnings when the `output.name` option is missing when needed.\n\nSee [`output.name`](https://rolldown.rs/reference/OutputOptions.name).",
              "markdownDescription": "Whether to emit warnings when the `output.name` option is missing when needed.\n\nSee [`output.name`](https://rolldown.rs/reference/OutputOptions.name).",
              "default": true
            },
            "mixedExports": {
              "type": "boolean",
              "description": "Whether to emit warnings when the way to export values is ambiguous.\n\nSee [`output.exports`](https://rolldown.rs/reference/OutputOptions.exports).",
              "markdownDescription": "Whether to emit warnings when the way to export values is ambiguous.\n\nSee [`output.exports`](https://rolldown.rs/reference/OutputOptions.exports).",
              "default": true
            },
            "unresolvedEntry": {
              "type": "boolean",
              "description": "Whether to emit warnings when an entrypoint cannot be resolved.",
              "markdownDescription": "Whether to emit warnings when an entrypoint cannot be resolved.",
              "default": true
            },
            "unresolvedImport": {
              "type": "boolean",
              "description": "Whether to emit warnings when an import cannot be resolved.",
              "markdownDescription": "Whether to emit warnings when an import cannot be resolved.",
              "default": true
            },
            "filenameConflict": {
              "type": "boolean",
              "description": "Whether to emit warnings when files generated have the same name with different contents.",
              "markdownDescription": "Whether to emit warnings when files generated have the same name with different contents.",
              "default": true
            },
            "commonJsVariableInEsm": {
              "type": "boolean",
              "description": "Whether to emit warnings when a CommonJS variable is used in an ES module.\n\nCommonJS variables like `module` and `exports` are treated as global variables in ES modules and may not work as expected.",
              "markdownDescription": "Whether to emit warnings when a CommonJS variable is used in an ES module.\n\nCommonJS variables like `module` and `exports` are treated as global variables in ES modules and may not work as expected.",
              "default": true
            },
            "importIsUndefined": {
              "type": "boolean",
              "description": "Whether to emit warnings when an imported variable is not exported.\n\nIf the code is importing a variable that is not exported by the imported module, the value will always be `undefined`. This might be a mistake in the code.",
              "markdownDescription": "Whether to emit warnings when an imported variable is not exported.\n\nIf the code is importing a variable that is not exported by the imported module, the value will always be `undefined`. This might be a mistake in the code.",
              "default": true
            },
            "emptyImportMeta": {
              "type": "boolean",
              "description": "Whether to emit warnings when `import.meta` is not supported with the output format and is replaced with an empty object (`{}`).\n\nSee [`import.meta` in Non-ESM Output Formats page](https://rolldown.rs/in-depth/non-esm-output-formats#import-meta) for more details.",
              "markdownDescription": "Whether to emit warnings when `import.meta` is not supported with the output format and is replaced with an empty object (`{}`).\n\nSee [`import.meta` in Non-ESM Output Formats page](https://rolldown.rs/in-depth/non-esm-output-formats#import-meta) for more details.",
              "default": true
            },
            "toleratedTransform": {
              "type": "boolean",
              "description": "Whether to emit warnings when detecting tolerated transform.",
              "markdownDescription": "Whether to emit warnings when detecting tolerated transform.",
              "default": true
            },
            "cannotCallNamespace": {
              "type": "boolean",
              "description": "Whether to emit warnings when a namespace is called as a function.\n\nA module namespace object is an object and not a function. Calling it as a function will cause a runtime error.",
              "markdownDescription": "Whether to emit warnings when a namespace is called as a function.\n\nA module namespace object is an object and not a function. Calling it as a function will cause a runtime error.",
              "default": true
            },
            "configurationFieldConflict": {
              "type": "boolean",
              "description": "Whether to emit warnings when a config value is overridden by another config value with a higher priority.",
              "markdownDescription": "Whether to emit warnings when a config value is overridden by another config value with a higher priority.",
              "default": true
            },
            "preferBuiltinFeature": {
              "type": "boolean",
              "description": "Whether to emit warnings when a plugin that is covered by a built-in feature is used.\n\nUsing built-in features is generally more performant than using plugins.",
              "markdownDescription": "Whether to emit warnings when a plugin that is covered by a built-in feature is used.\n\nUsing built-in features is generally more performant than using plugins.",
              "default": true
            },
            "couldNotCleanDirectory": {
              "type": "boolean",
              "description": "Whether to emit warnings when Rolldown could not clean the output directory.\n\nSee [`output.cleanDir`](https://rolldown.rs/reference/OutputOptions.cleanDir).",
              "markdownDescription": "Whether to emit warnings when Rolldown could not clean the output directory.\n\nSee [`output.cleanDir`](https://rolldown.rs/reference/OutputOptions.cleanDir).",
              "default": true
            },
            "pluginTimings": {
              "type": "boolean",
              "description": "Whether to emit warnings when plugins take significant time during the build process.",
              "markdownDescription": "Whether to emit warnings when plugins take significant time during the build process.",
              "default": true
            },
            "duplicateShebang": {
              "type": "boolean",
              "description": "Whether to emit warnings when both the code and postBanner contain shebang\n\nHaving multiple shebangs in a file is a syntax error.",
              "markdownDescription": "Whether to emit warnings when both the code and postBanner contain shebang\n\nHaving multiple shebangs in a file is a syntax error.",
              "default": true
            },
            "unsupportedTsconfigOption": {
              "type": "boolean",
              "description": "Whether to emit warnings when a tsconfig option or combination of options is not supported.",
              "markdownDescription": "Whether to emit warnings when a tsconfig option or combination of options is not supported.",
              "default": true
            },
            "ineffectiveDynamicImport": {
              "type": "boolean",
              "description": "Whether to emit warnings when a module is dynamically imported but also statically imported, making the dynamic import ineffective for code splitting.",
              "markdownDescription": "Whether to emit warnings when a module is dynamically imported but also statically imported, making the dynamic import ineffective for code splitting.",
              "default": true
            }
          },
          "description": "Controls which warnings are emitted during the build process. Each option can be set to `true` (emit warning) or `false` (suppress warning).",
          "markdownDescription": "Controls which warnings are emitted during the build process. Each option can be set to `true` (emit warning) or `false` (suppress warning)."
        },
        "format": {
          "anyOf": [
            { "$ref": "#/definitions/Arrayable%3CFormat%3E" },
            {
              "type": "object",
              "properties": {
                "es": {
                  "type": "object",
                  "properties": {
                    "globalName": { "not": {} },
                    "inputOptions": { "not": {} },
                    "outputOptions": { "not": {} },
                    "minify": { "not": {} },
                    "define": { "not": {} },
                    "alias": { "not": {} },
                    "onSuccess": { "not": {} },
                    "outExtensions": { "not": {} },
                    "hooks": { "not": {} },
                    "copy": { "not": {} },
                    "loader": { "not": {} },
                    "name": { "not": {} },
                    "banner": { "not": {} },
                    "footer": { "not": {} },
                    "checks": { "not": {} },
                    "css": { "not": {} },
                    "entry": {
                      "type": "object",
                      "additionalProperties": { "type": "string" },
                      "description": "Resolved entry map (after glob expansion)",
                      "markdownDescription": "Resolved entry map (after glob expansion)"
                    },
                    "rawEntry": {
                      "$ref": "#/definitions/TsdownInputOption",
                      "description": "Original entry config before glob resolution (for watch mode re-globbing)",
                      "markdownDescription": "Original entry config before glob resolution (for watch mode re-globbing)"
                    },
                    "nameLabel": { "type": "string" },
                    "format": { "$ref": "#/definitions/NormalizedFormat" },
                    "target": {
                      "type": "array",
                      "items": { "type": "string" }
                    },
                    "clean": { "type": "array", "items": { "type": "string" } },
                    "pkg": { "$ref": "#/definitions/PackageJsonWithPath" },
                    "nodeProtocol": {
                      "anyOf": [
                        { "type": "string", "const": "strip" },
                        { "type": "boolean" }
                      ]
                    },
                    "ignoreWatch": {
                      "type": "array",
                      "items": {
                        "anyOf": [
                          { "type": "string" },
                          { "type": "string", "format": "regex" }
                        ]
                      }
                    },
                    "deps": { "$ref": "#/definitions/ResolvedDepsConfig" },
                    "root": {
                      "type": "string",
                      "description": "Resolved root directory of input files",
                      "markdownDescription": "Resolved root directory of input files"
                    },
                    "configDeps": {
                      "type": "object",
                      "properties": { "size": { "type": "number" } },
                      "required": ["size"],
                      "additionalProperties": false
                    },
                    "dts": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/DtsOptions" }
                      ]
                    },
                    "report": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/ReportOptions" }
                      ]
                    },
                    "tsconfig": {
                      "anyOf": [
                        { "type": "string" },
                        { "type": "boolean", "const": false }
                      ]
                    },
                    "exports": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/ExportsOptions" }
                      ]
                    },
                    "devtools": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/DevtoolsOptions" }
                      ]
                    },
                    "publint": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/PublintOptions" }
                      ]
                    },
                    "attw": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/AttwOptions" }
                      ]
                    },
                    "unused": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        {
                          "type": "object",
                          "properties": {
                            "root": { "type": "string" },
                            "include": {
                              "anyOf": [
                                { "type": "string" },
                                { "type": "string", "format": "regex" },
                                {
                                  "type": "array",
                                  "items": {
                                    "anyOf": [
                                      { "type": "string" },
                                      { "type": "string", "format": "regex" }
                                    ]
                                  }
                                }
                              ],
                              "description": "Array, or not yet",
                              "markdownDescription": "Array, or not yet"
                            },
                            "exclude": {
                              "anyOf": [
                                { "type": "string" },
                                { "type": "string", "format": "regex" },
                                {
                                  "type": "array",
                                  "items": {
                                    "anyOf": [
                                      { "type": "string" },
                                      { "type": "string", "format": "regex" }
                                    ]
                                  }
                                }
                              ],
                              "description": "Array, or not yet",
                              "markdownDescription": "Array, or not yet"
                            },
                            "ignore": {
                              "anyOf": [
                                {
                                  "type": "array",
                                  "items": { "type": "string" }
                                },
                                {
                                  "type": "object",
                                  "properties": {
                                    "dependencies": {
                                      "type": "array",
                                      "items": { "type": "string" }
                                    },
                                    "devDependencies": {
                                      "type": "array",
                                      "items": { "type": "string" }
                                    },
                                    "peerDependencies": {
                                      "type": "array",
                                      "items": { "type": "string" }
                                    }
                                  },
                                  "additionalProperties": false
                                }
                              ]
                            },
                            "level": {
                              "type": "string",
                              "enum": ["warning", "error"],
                              "description": "Specifies the severity level of the check.\n\n- `'error'`: Causes the build to fail.\n- `'warning'`: Displays a warning in the console.",
                              "markdownDescription": "Specifies the severity level of the check.\n\n- `'error'`: Causes the build to fail.\n- `'warning'`: Displays a warning in the console.",
                              "default": "warning"
                            },
                            "depKinds": {
                              "type": "array",
                              "items": {
                                "type": "string",
                                "enum": [
                                  "dependencies",
                                  "devDependencies",
                                  "peerDependencies"
                                ]
                              },
                              "default": ["dependencies", "peerDependencies"]
                            }
                          },
                          "additionalProperties": false
                        }
                      ]
                    },
                    "exe": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/ExeOptions" }
                      ]
                    }
                  },
                  "additionalProperties": false
                },
                "cjs": {
                  "type": "object",
                  "properties": {
                    "globalName": { "not": {} },
                    "inputOptions": { "not": {} },
                    "outputOptions": { "not": {} },
                    "minify": { "not": {} },
                    "define": { "not": {} },
                    "alias": { "not": {} },
                    "onSuccess": { "not": {} },
                    "outExtensions": { "not": {} },
                    "hooks": { "not": {} },
                    "copy": { "not": {} },
                    "loader": { "not": {} },
                    "name": { "not": {} },
                    "banner": { "not": {} },
                    "footer": { "not": {} },
                    "checks": { "not": {} },
                    "css": { "not": {} },
                    "entry": {
                      "type": "object",
                      "additionalProperties": { "type": "string" },
                      "description": "Resolved entry map (after glob expansion)",
                      "markdownDescription": "Resolved entry map (after glob expansion)"
                    },
                    "rawEntry": {
                      "$ref": "#/definitions/TsdownInputOption",
                      "description": "Original entry config before glob resolution (for watch mode re-globbing)",
                      "markdownDescription": "Original entry config before glob resolution (for watch mode re-globbing)"
                    },
                    "nameLabel": { "type": "string" },
                    "format": { "$ref": "#/definitions/NormalizedFormat" },
                    "target": {
                      "type": "array",
                      "items": { "type": "string" }
                    },
                    "clean": { "type": "array", "items": { "type": "string" } },
                    "pkg": { "$ref": "#/definitions/PackageJsonWithPath" },
                    "nodeProtocol": {
                      "anyOf": [
                        { "type": "string", "const": "strip" },
                        { "type": "boolean" }
                      ]
                    },
                    "ignoreWatch": {
                      "type": "array",
                      "items": {
                        "anyOf": [
                          { "type": "string" },
                          { "type": "string", "format": "regex" }
                        ]
                      }
                    },
                    "deps": { "$ref": "#/definitions/ResolvedDepsConfig" },
                    "root": {
                      "type": "string",
                      "description": "Resolved root directory of input files",
                      "markdownDescription": "Resolved root directory of input files"
                    },
                    "configDeps": {
                      "type": "object",
                      "properties": { "size": { "type": "number" } },
                      "required": ["size"],
                      "additionalProperties": false
                    },
                    "dts": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/DtsOptions" }
                      ]
                    },
                    "report": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/ReportOptions" }
                      ]
                    },
                    "tsconfig": {
                      "anyOf": [
                        { "type": "string" },
                        { "type": "boolean", "const": false }
                      ]
                    },
                    "exports": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/ExportsOptions" }
                      ]
                    },
                    "devtools": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/DevtoolsOptions" }
                      ]
                    },
                    "publint": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/PublintOptions" }
                      ]
                    },
                    "attw": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/AttwOptions" }
                      ]
                    },
                    "unused": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        {
                          "type": "object",
                          "properties": {
                            "root": { "type": "string" },
                            "include": {
                              "anyOf": [
                                { "type": "string" },
                                { "type": "string", "format": "regex" },
                                {
                                  "type": "array",
                                  "items": {
                                    "anyOf": [
                                      { "type": "string" },
                                      { "type": "string", "format": "regex" }
                                    ]
                                  }
                                }
                              ],
                              "description": "Array, or not yet",
                              "markdownDescription": "Array, or not yet"
                            },
                            "exclude": {
                              "anyOf": [
                                { "type": "string" },
                                { "type": "string", "format": "regex" },
                                {
                                  "type": "array",
                                  "items": {
                                    "anyOf": [
                                      { "type": "string" },
                                      { "type": "string", "format": "regex" }
                                    ]
                                  }
                                }
                              ],
                              "description": "Array, or not yet",
                              "markdownDescription": "Array, or not yet"
                            },
                            "ignore": {
                              "anyOf": [
                                {
                                  "type": "array",
                                  "items": { "type": "string" }
                                },
                                {
                                  "type": "object",
                                  "properties": {
                                    "dependencies": {
                                      "type": "array",
                                      "items": { "type": "string" }
                                    },
                                    "devDependencies": {
                                      "type": "array",
                                      "items": { "type": "string" }
                                    },
                                    "peerDependencies": {
                                      "type": "array",
                                      "items": { "type": "string" }
                                    }
                                  },
                                  "additionalProperties": false
                                }
                              ]
                            },
                            "level": {
                              "type": "string",
                              "enum": ["warning", "error"],
                              "description": "Specifies the severity level of the check.\n\n- `'error'`: Causes the build to fail.\n- `'warning'`: Displays a warning in the console.",
                              "markdownDescription": "Specifies the severity level of the check.\n\n- `'error'`: Causes the build to fail.\n- `'warning'`: Displays a warning in the console.",
                              "default": "warning"
                            },
                            "depKinds": {
                              "type": "array",
                              "items": {
                                "type": "string",
                                "enum": [
                                  "dependencies",
                                  "devDependencies",
                                  "peerDependencies"
                                ]
                              },
                              "default": ["dependencies", "peerDependencies"]
                            }
                          },
                          "additionalProperties": false
                        }
                      ]
                    },
                    "exe": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/ExeOptions" }
                      ]
                    }
                  },
                  "additionalProperties": false
                },
                "esm": {
                  "type": "object",
                  "properties": {
                    "globalName": { "not": {} },
                    "inputOptions": { "not": {} },
                    "outputOptions": { "not": {} },
                    "minify": { "not": {} },
                    "define": { "not": {} },
                    "alias": { "not": {} },
                    "onSuccess": { "not": {} },
                    "outExtensions": { "not": {} },
                    "hooks": { "not": {} },
                    "copy": { "not": {} },
                    "loader": { "not": {} },
                    "name": { "not": {} },
                    "banner": { "not": {} },
                    "footer": { "not": {} },
                    "checks": { "not": {} },
                    "css": { "not": {} },
                    "entry": {
                      "type": "object",
                      "additionalProperties": { "type": "string" },
                      "description": "Resolved entry map (after glob expansion)",
                      "markdownDescription": "Resolved entry map (after glob expansion)"
                    },
                    "rawEntry": {
                      "$ref": "#/definitions/TsdownInputOption",
                      "description": "Original entry config before glob resolution (for watch mode re-globbing)",
                      "markdownDescription": "Original entry config before glob resolution (for watch mode re-globbing)"
                    },
                    "nameLabel": { "type": "string" },
                    "format": { "$ref": "#/definitions/NormalizedFormat" },
                    "target": {
                      "type": "array",
                      "items": { "type": "string" }
                    },
                    "clean": { "type": "array", "items": { "type": "string" } },
                    "pkg": { "$ref": "#/definitions/PackageJsonWithPath" },
                    "nodeProtocol": {
                      "anyOf": [
                        { "type": "string", "const": "strip" },
                        { "type": "boolean" }
                      ]
                    },
                    "ignoreWatch": {
                      "type": "array",
                      "items": {
                        "anyOf": [
                          { "type": "string" },
                          { "type": "string", "format": "regex" }
                        ]
                      }
                    },
                    "deps": { "$ref": "#/definitions/ResolvedDepsConfig" },
                    "root": {
                      "type": "string",
                      "description": "Resolved root directory of input files",
                      "markdownDescription": "Resolved root directory of input files"
                    },
                    "configDeps": {
                      "type": "object",
                      "properties": { "size": { "type": "number" } },
                      "required": ["size"],
                      "additionalProperties": false
                    },
                    "dts": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/DtsOptions" }
                      ]
                    },
                    "report": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/ReportOptions" }
                      ]
                    },
                    "tsconfig": {
                      "anyOf": [
                        { "type": "string" },
                        { "type": "boolean", "const": false }
                      ]
                    },
                    "exports": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/ExportsOptions" }
                      ]
                    },
                    "devtools": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/DevtoolsOptions" }
                      ]
                    },
                    "publint": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/PublintOptions" }
                      ]
                    },
                    "attw": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/AttwOptions" }
                      ]
                    },
                    "unused": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        {
                          "type": "object",
                          "properties": {
                            "root": { "type": "string" },
                            "include": {
                              "anyOf": [
                                { "type": "string" },
                                { "type": "string", "format": "regex" },
                                {
                                  "type": "array",
                                  "items": {
                                    "anyOf": [
                                      { "type": "string" },
                                      { "type": "string", "format": "regex" }
                                    ]
                                  }
                                }
                              ],
                              "description": "Array, or not yet",
                              "markdownDescription": "Array, or not yet"
                            },
                            "exclude": {
                              "anyOf": [
                                { "type": "string" },
                                { "type": "string", "format": "regex" },
                                {
                                  "type": "array",
                                  "items": {
                                    "anyOf": [
                                      { "type": "string" },
                                      { "type": "string", "format": "regex" }
                                    ]
                                  }
                                }
                              ],
                              "description": "Array, or not yet",
                              "markdownDescription": "Array, or not yet"
                            },
                            "ignore": {
                              "anyOf": [
                                {
                                  "type": "array",
                                  "items": { "type": "string" }
                                },
                                {
                                  "type": "object",
                                  "properties": {
                                    "dependencies": {
                                      "type": "array",
                                      "items": { "type": "string" }
                                    },
                                    "devDependencies": {
                                      "type": "array",
                                      "items": { "type": "string" }
                                    },
                                    "peerDependencies": {
                                      "type": "array",
                                      "items": { "type": "string" }
                                    }
                                  },
                                  "additionalProperties": false
                                }
                              ]
                            },
                            "level": {
                              "type": "string",
                              "enum": ["warning", "error"],
                              "description": "Specifies the severity level of the check.\n\n- `'error'`: Causes the build to fail.\n- `'warning'`: Displays a warning in the console.",
                              "markdownDescription": "Specifies the severity level of the check.\n\n- `'error'`: Causes the build to fail.\n- `'warning'`: Displays a warning in the console.",
                              "default": "warning"
                            },
                            "depKinds": {
                              "type": "array",
                              "items": {
                                "type": "string",
                                "enum": [
                                  "dependencies",
                                  "devDependencies",
                                  "peerDependencies"
                                ]
                              },
                              "default": ["dependencies", "peerDependencies"]
                            }
                          },
                          "additionalProperties": false
                        }
                      ]
                    },
                    "exe": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/ExeOptions" }
                      ]
                    }
                  },
                  "additionalProperties": false
                },
                "module": {
                  "type": "object",
                  "properties": {
                    "globalName": { "not": {} },
                    "inputOptions": { "not": {} },
                    "outputOptions": { "not": {} },
                    "minify": { "not": {} },
                    "define": { "not": {} },
                    "alias": { "not": {} },
                    "onSuccess": { "not": {} },
                    "outExtensions": { "not": {} },
                    "hooks": { "not": {} },
                    "copy": { "not": {} },
                    "loader": { "not": {} },
                    "name": { "not": {} },
                    "banner": { "not": {} },
                    "footer": { "not": {} },
                    "checks": { "not": {} },
                    "css": { "not": {} },
                    "entry": {
                      "type": "object",
                      "additionalProperties": { "type": "string" },
                      "description": "Resolved entry map (after glob expansion)",
                      "markdownDescription": "Resolved entry map (after glob expansion)"
                    },
                    "rawEntry": {
                      "$ref": "#/definitions/TsdownInputOption",
                      "description": "Original entry config before glob resolution (for watch mode re-globbing)",
                      "markdownDescription": "Original entry config before glob resolution (for watch mode re-globbing)"
                    },
                    "nameLabel": { "type": "string" },
                    "format": { "$ref": "#/definitions/NormalizedFormat" },
                    "target": {
                      "type": "array",
                      "items": { "type": "string" }
                    },
                    "clean": { "type": "array", "items": { "type": "string" } },
                    "pkg": { "$ref": "#/definitions/PackageJsonWithPath" },
                    "nodeProtocol": {
                      "anyOf": [
                        { "type": "string", "const": "strip" },
                        { "type": "boolean" }
                      ]
                    },
                    "ignoreWatch": {
                      "type": "array",
                      "items": {
                        "anyOf": [
                          { "type": "string" },
                          { "type": "string", "format": "regex" }
                        ]
                      }
                    },
                    "deps": { "$ref": "#/definitions/ResolvedDepsConfig" },
                    "root": {
                      "type": "string",
                      "description": "Resolved root directory of input files",
                      "markdownDescription": "Resolved root directory of input files"
                    },
                    "configDeps": {
                      "type": "object",
                      "properties": { "size": { "type": "number" } },
                      "required": ["size"],
                      "additionalProperties": false
                    },
                    "dts": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/DtsOptions" }
                      ]
                    },
                    "report": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/ReportOptions" }
                      ]
                    },
                    "tsconfig": {
                      "anyOf": [
                        { "type": "string" },
                        { "type": "boolean", "const": false }
                      ]
                    },
                    "exports": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/ExportsOptions" }
                      ]
                    },
                    "devtools": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/DevtoolsOptions" }
                      ]
                    },
                    "publint": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/PublintOptions" }
                      ]
                    },
                    "attw": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/AttwOptions" }
                      ]
                    },
                    "unused": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        {
                          "type": "object",
                          "properties": {
                            "root": { "type": "string" },
                            "include": {
                              "anyOf": [
                                { "type": "string" },
                                { "type": "string", "format": "regex" },
                                {
                                  "type": "array",
                                  "items": {
                                    "anyOf": [
                                      { "type": "string" },
                                      { "type": "string", "format": "regex" }
                                    ]
                                  }
                                }
                              ],
                              "description": "Array, or not yet",
                              "markdownDescription": "Array, or not yet"
                            },
                            "exclude": {
                              "anyOf": [
                                { "type": "string" },
                                { "type": "string", "format": "regex" },
                                {
                                  "type": "array",
                                  "items": {
                                    "anyOf": [
                                      { "type": "string" },
                                      { "type": "string", "format": "regex" }
                                    ]
                                  }
                                }
                              ],
                              "description": "Array, or not yet",
                              "markdownDescription": "Array, or not yet"
                            },
                            "ignore": {
                              "anyOf": [
                                {
                                  "type": "array",
                                  "items": { "type": "string" }
                                },
                                {
                                  "type": "object",
                                  "properties": {
                                    "dependencies": {
                                      "type": "array",
                                      "items": { "type": "string" }
                                    },
                                    "devDependencies": {
                                      "type": "array",
                                      "items": { "type": "string" }
                                    },
                                    "peerDependencies": {
                                      "type": "array",
                                      "items": { "type": "string" }
                                    }
                                  },
                                  "additionalProperties": false
                                }
                              ]
                            },
                            "level": {
                              "type": "string",
                              "enum": ["warning", "error"],
                              "description": "Specifies the severity level of the check.\n\n- `'error'`: Causes the build to fail.\n- `'warning'`: Displays a warning in the console.",
                              "markdownDescription": "Specifies the severity level of the check.\n\n- `'error'`: Causes the build to fail.\n- `'warning'`: Displays a warning in the console.",
                              "default": "warning"
                            },
                            "depKinds": {
                              "type": "array",
                              "items": {
                                "type": "string",
                                "enum": [
                                  "dependencies",
                                  "devDependencies",
                                  "peerDependencies"
                                ]
                              },
                              "default": ["dependencies", "peerDependencies"]
                            }
                          },
                          "additionalProperties": false
                        }
                      ]
                    },
                    "exe": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/ExeOptions" }
                      ]
                    }
                  },
                  "additionalProperties": false
                },
                "commonjs": {
                  "type": "object",
                  "properties": {
                    "globalName": { "not": {} },
                    "inputOptions": { "not": {} },
                    "outputOptions": { "not": {} },
                    "minify": { "not": {} },
                    "define": { "not": {} },
                    "alias": { "not": {} },
                    "onSuccess": { "not": {} },
                    "outExtensions": { "not": {} },
                    "hooks": { "not": {} },
                    "copy": { "not": {} },
                    "loader": { "not": {} },
                    "name": { "not": {} },
                    "banner": { "not": {} },
                    "footer": { "not": {} },
                    "checks": { "not": {} },
                    "css": { "not": {} },
                    "entry": {
                      "type": "object",
                      "additionalProperties": { "type": "string" },
                      "description": "Resolved entry map (after glob expansion)",
                      "markdownDescription": "Resolved entry map (after glob expansion)"
                    },
                    "rawEntry": {
                      "$ref": "#/definitions/TsdownInputOption",
                      "description": "Original entry config before glob resolution (for watch mode re-globbing)",
                      "markdownDescription": "Original entry config before glob resolution (for watch mode re-globbing)"
                    },
                    "nameLabel": { "type": "string" },
                    "format": { "$ref": "#/definitions/NormalizedFormat" },
                    "target": {
                      "type": "array",
                      "items": { "type": "string" }
                    },
                    "clean": { "type": "array", "items": { "type": "string" } },
                    "pkg": { "$ref": "#/definitions/PackageJsonWithPath" },
                    "nodeProtocol": {
                      "anyOf": [
                        { "type": "string", "const": "strip" },
                        { "type": "boolean" }
                      ]
                    },
                    "ignoreWatch": {
                      "type": "array",
                      "items": {
                        "anyOf": [
                          { "type": "string" },
                          { "type": "string", "format": "regex" }
                        ]
                      }
                    },
                    "deps": { "$ref": "#/definitions/ResolvedDepsConfig" },
                    "root": {
                      "type": "string",
                      "description": "Resolved root directory of input files",
                      "markdownDescription": "Resolved root directory of input files"
                    },
                    "configDeps": {
                      "type": "object",
                      "properties": { "size": { "type": "number" } },
                      "required": ["size"],
                      "additionalProperties": false
                    },
                    "dts": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/DtsOptions" }
                      ]
                    },
                    "report": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/ReportOptions" }
                      ]
                    },
                    "tsconfig": {
                      "anyOf": [
                        { "type": "string" },
                        { "type": "boolean", "const": false }
                      ]
                    },
                    "exports": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/ExportsOptions" }
                      ]
                    },
                    "devtools": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/DevtoolsOptions" }
                      ]
                    },
                    "publint": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/PublintOptions" }
                      ]
                    },
                    "attw": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/AttwOptions" }
                      ]
                    },
                    "unused": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        {
                          "type": "object",
                          "properties": {
                            "root": { "type": "string" },
                            "include": {
                              "anyOf": [
                                { "type": "string" },
                                { "type": "string", "format": "regex" },
                                {
                                  "type": "array",
                                  "items": {
                                    "anyOf": [
                                      { "type": "string" },
                                      { "type": "string", "format": "regex" }
                                    ]
                                  }
                                }
                              ],
                              "description": "Array, or not yet",
                              "markdownDescription": "Array, or not yet"
                            },
                            "exclude": {
                              "anyOf": [
                                { "type": "string" },
                                { "type": "string", "format": "regex" },
                                {
                                  "type": "array",
                                  "items": {
                                    "anyOf": [
                                      { "type": "string" },
                                      { "type": "string", "format": "regex" }
                                    ]
                                  }
                                }
                              ],
                              "description": "Array, or not yet",
                              "markdownDescription": "Array, or not yet"
                            },
                            "ignore": {
                              "anyOf": [
                                {
                                  "type": "array",
                                  "items": { "type": "string" }
                                },
                                {
                                  "type": "object",
                                  "properties": {
                                    "dependencies": {
                                      "type": "array",
                                      "items": { "type": "string" }
                                    },
                                    "devDependencies": {
                                      "type": "array",
                                      "items": { "type": "string" }
                                    },
                                    "peerDependencies": {
                                      "type": "array",
                                      "items": { "type": "string" }
                                    }
                                  },
                                  "additionalProperties": false
                                }
                              ]
                            },
                            "level": {
                              "type": "string",
                              "enum": ["warning", "error"],
                              "description": "Specifies the severity level of the check.\n\n- `'error'`: Causes the build to fail.\n- `'warning'`: Displays a warning in the console.",
                              "markdownDescription": "Specifies the severity level of the check.\n\n- `'error'`: Causes the build to fail.\n- `'warning'`: Displays a warning in the console.",
                              "default": "warning"
                            },
                            "depKinds": {
                              "type": "array",
                              "items": {
                                "type": "string",
                                "enum": [
                                  "dependencies",
                                  "devDependencies",
                                  "peerDependencies"
                                ]
                              },
                              "default": ["dependencies", "peerDependencies"]
                            }
                          },
                          "additionalProperties": false
                        }
                      ]
                    },
                    "exe": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/ExeOptions" }
                      ]
                    }
                  },
                  "additionalProperties": false
                },
                "iife": {
                  "type": "object",
                  "properties": {
                    "globalName": { "not": {} },
                    "inputOptions": { "not": {} },
                    "outputOptions": { "not": {} },
                    "minify": { "not": {} },
                    "define": { "not": {} },
                    "alias": { "not": {} },
                    "onSuccess": { "not": {} },
                    "outExtensions": { "not": {} },
                    "hooks": { "not": {} },
                    "copy": { "not": {} },
                    "loader": { "not": {} },
                    "name": { "not": {} },
                    "banner": { "not": {} },
                    "footer": { "not": {} },
                    "checks": { "not": {} },
                    "css": { "not": {} },
                    "entry": {
                      "type": "object",
                      "additionalProperties": { "type": "string" },
                      "description": "Resolved entry map (after glob expansion)",
                      "markdownDescription": "Resolved entry map (after glob expansion)"
                    },
                    "rawEntry": {
                      "$ref": "#/definitions/TsdownInputOption",
                      "description": "Original entry config before glob resolution (for watch mode re-globbing)",
                      "markdownDescription": "Original entry config before glob resolution (for watch mode re-globbing)"
                    },
                    "nameLabel": { "type": "string" },
                    "format": { "$ref": "#/definitions/NormalizedFormat" },
                    "target": {
                      "type": "array",
                      "items": { "type": "string" }
                    },
                    "clean": { "type": "array", "items": { "type": "string" } },
                    "pkg": { "$ref": "#/definitions/PackageJsonWithPath" },
                    "nodeProtocol": {
                      "anyOf": [
                        { "type": "string", "const": "strip" },
                        { "type": "boolean" }
                      ]
                    },
                    "ignoreWatch": {
                      "type": "array",
                      "items": {
                        "anyOf": [
                          { "type": "string" },
                          { "type": "string", "format": "regex" }
                        ]
                      }
                    },
                    "deps": { "$ref": "#/definitions/ResolvedDepsConfig" },
                    "root": {
                      "type": "string",
                      "description": "Resolved root directory of input files",
                      "markdownDescription": "Resolved root directory of input files"
                    },
                    "configDeps": {
                      "type": "object",
                      "properties": { "size": { "type": "number" } },
                      "required": ["size"],
                      "additionalProperties": false
                    },
                    "dts": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/DtsOptions" }
                      ]
                    },
                    "report": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/ReportOptions" }
                      ]
                    },
                    "tsconfig": {
                      "anyOf": [
                        { "type": "string" },
                        { "type": "boolean", "const": false }
                      ]
                    },
                    "exports": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/ExportsOptions" }
                      ]
                    },
                    "devtools": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/DevtoolsOptions" }
                      ]
                    },
                    "publint": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/PublintOptions" }
                      ]
                    },
                    "attw": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/AttwOptions" }
                      ]
                    },
                    "unused": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        {
                          "type": "object",
                          "properties": {
                            "root": { "type": "string" },
                            "include": {
                              "anyOf": [
                                { "type": "string" },
                                { "type": "string", "format": "regex" },
                                {
                                  "type": "array",
                                  "items": {
                                    "anyOf": [
                                      { "type": "string" },
                                      { "type": "string", "format": "regex" }
                                    ]
                                  }
                                }
                              ],
                              "description": "Array, or not yet",
                              "markdownDescription": "Array, or not yet"
                            },
                            "exclude": {
                              "anyOf": [
                                { "type": "string" },
                                { "type": "string", "format": "regex" },
                                {
                                  "type": "array",
                                  "items": {
                                    "anyOf": [
                                      { "type": "string" },
                                      { "type": "string", "format": "regex" }
                                    ]
                                  }
                                }
                              ],
                              "description": "Array, or not yet",
                              "markdownDescription": "Array, or not yet"
                            },
                            "ignore": {
                              "anyOf": [
                                {
                                  "type": "array",
                                  "items": { "type": "string" }
                                },
                                {
                                  "type": "object",
                                  "properties": {
                                    "dependencies": {
                                      "type": "array",
                                      "items": { "type": "string" }
                                    },
                                    "devDependencies": {
                                      "type": "array",
                                      "items": { "type": "string" }
                                    },
                                    "peerDependencies": {
                                      "type": "array",
                                      "items": { "type": "string" }
                                    }
                                  },
                                  "additionalProperties": false
                                }
                              ]
                            },
                            "level": {
                              "type": "string",
                              "enum": ["warning", "error"],
                              "description": "Specifies the severity level of the check.\n\n- `'error'`: Causes the build to fail.\n- `'warning'`: Displays a warning in the console.",
                              "markdownDescription": "Specifies the severity level of the check.\n\n- `'error'`: Causes the build to fail.\n- `'warning'`: Displays a warning in the console.",
                              "default": "warning"
                            },
                            "depKinds": {
                              "type": "array",
                              "items": {
                                "type": "string",
                                "enum": [
                                  "dependencies",
                                  "devDependencies",
                                  "peerDependencies"
                                ]
                              },
                              "default": ["dependencies", "peerDependencies"]
                            }
                          },
                          "additionalProperties": false
                        }
                      ]
                    },
                    "exe": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/ExeOptions" }
                      ]
                    }
                  },
                  "additionalProperties": false
                },
                "umd": {
                  "type": "object",
                  "properties": {
                    "globalName": { "not": {} },
                    "inputOptions": { "not": {} },
                    "outputOptions": { "not": {} },
                    "minify": { "not": {} },
                    "define": { "not": {} },
                    "alias": { "not": {} },
                    "onSuccess": { "not": {} },
                    "outExtensions": { "not": {} },
                    "hooks": { "not": {} },
                    "copy": { "not": {} },
                    "loader": { "not": {} },
                    "name": { "not": {} },
                    "banner": { "not": {} },
                    "footer": { "not": {} },
                    "checks": { "not": {} },
                    "css": { "not": {} },
                    "entry": {
                      "type": "object",
                      "additionalProperties": { "type": "string" },
                      "description": "Resolved entry map (after glob expansion)",
                      "markdownDescription": "Resolved entry map (after glob expansion)"
                    },
                    "rawEntry": {
                      "$ref": "#/definitions/TsdownInputOption",
                      "description": "Original entry config before glob resolution (for watch mode re-globbing)",
                      "markdownDescription": "Original entry config before glob resolution (for watch mode re-globbing)"
                    },
                    "nameLabel": { "type": "string" },
                    "format": { "$ref": "#/definitions/NormalizedFormat" },
                    "target": {
                      "type": "array",
                      "items": { "type": "string" }
                    },
                    "clean": { "type": "array", "items": { "type": "string" } },
                    "pkg": { "$ref": "#/definitions/PackageJsonWithPath" },
                    "nodeProtocol": {
                      "anyOf": [
                        { "type": "string", "const": "strip" },
                        { "type": "boolean" }
                      ]
                    },
                    "ignoreWatch": {
                      "type": "array",
                      "items": {
                        "anyOf": [
                          { "type": "string" },
                          { "type": "string", "format": "regex" }
                        ]
                      }
                    },
                    "deps": { "$ref": "#/definitions/ResolvedDepsConfig" },
                    "root": {
                      "type": "string",
                      "description": "Resolved root directory of input files",
                      "markdownDescription": "Resolved root directory of input files"
                    },
                    "configDeps": {
                      "type": "object",
                      "properties": { "size": { "type": "number" } },
                      "required": ["size"],
                      "additionalProperties": false
                    },
                    "dts": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/DtsOptions" }
                      ]
                    },
                    "report": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/ReportOptions" }
                      ]
                    },
                    "tsconfig": {
                      "anyOf": [
                        { "type": "string" },
                        { "type": "boolean", "const": false }
                      ]
                    },
                    "exports": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/ExportsOptions" }
                      ]
                    },
                    "devtools": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/DevtoolsOptions" }
                      ]
                    },
                    "publint": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/PublintOptions" }
                      ]
                    },
                    "attw": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/AttwOptions" }
                      ]
                    },
                    "unused": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        {
                          "type": "object",
                          "properties": {
                            "root": { "type": "string" },
                            "include": {
                              "anyOf": [
                                { "type": "string" },
                                { "type": "string", "format": "regex" },
                                {
                                  "type": "array",
                                  "items": {
                                    "anyOf": [
                                      { "type": "string" },
                                      { "type": "string", "format": "regex" }
                                    ]
                                  }
                                }
                              ],
                              "description": "Array, or not yet",
                              "markdownDescription": "Array, or not yet"
                            },
                            "exclude": {
                              "anyOf": [
                                { "type": "string" },
                                { "type": "string", "format": "regex" },
                                {
                                  "type": "array",
                                  "items": {
                                    "anyOf": [
                                      { "type": "string" },
                                      { "type": "string", "format": "regex" }
                                    ]
                                  }
                                }
                              ],
                              "description": "Array, or not yet",
                              "markdownDescription": "Array, or not yet"
                            },
                            "ignore": {
                              "anyOf": [
                                {
                                  "type": "array",
                                  "items": { "type": "string" }
                                },
                                {
                                  "type": "object",
                                  "properties": {
                                    "dependencies": {
                                      "type": "array",
                                      "items": { "type": "string" }
                                    },
                                    "devDependencies": {
                                      "type": "array",
                                      "items": { "type": "string" }
                                    },
                                    "peerDependencies": {
                                      "type": "array",
                                      "items": { "type": "string" }
                                    }
                                  },
                                  "additionalProperties": false
                                }
                              ]
                            },
                            "level": {
                              "type": "string",
                              "enum": ["warning", "error"],
                              "description": "Specifies the severity level of the check.\n\n- `'error'`: Causes the build to fail.\n- `'warning'`: Displays a warning in the console.",
                              "markdownDescription": "Specifies the severity level of the check.\n\n- `'error'`: Causes the build to fail.\n- `'warning'`: Displays a warning in the console.",
                              "default": "warning"
                            },
                            "depKinds": {
                              "type": "array",
                              "items": {
                                "type": "string",
                                "enum": [
                                  "dependencies",
                                  "devDependencies",
                                  "peerDependencies"
                                ]
                              },
                              "default": ["dependencies", "peerDependencies"]
                            }
                          },
                          "additionalProperties": false
                        }
                      ]
                    },
                    "exe": {
                      "anyOf": [
                        { "type": "boolean", "const": false },
                        { "$ref": "#/definitions/ExeOptions" }
                      ]
                    }
                  },
                  "additionalProperties": false
                }
              },
              "additionalProperties": false
            }
          ],
          "description": "Output format(s). Available formats are\n- `esm`: ESM\n- `cjs`: CommonJS\n- `iife`: IIFE\n- `umd`: UMD",
          "markdownDescription": "Output format(s). Available formats are\n- `esm`: ESM\n- `cjs`: CommonJS\n- `iife`: IIFE\n- `umd`: UMD",
          "default": "esm"
        },
        "globalName": {
          "type": "string",
          "description": "Specifies the global variable name that contains the exports of `umd` / `iife`  {@link  OutputOptions.format formats } .",
          "markdownDescription": "Specifies the global variable name that contains the exports of `umd` / `iife`  {@link  OutputOptions.format formats } ."
        },
        "outDir": { "type": "string", "default": "dist" },
        "write": {
          "type": "boolean",
          "description": "Whether to write the files to disk. This option is incompatible with watch mode.",
          "markdownDescription": "Whether to write the files to disk.\nThis option is incompatible with watch mode.",
          "default": true
        },
        "sourcemap": {
          "anyOf": [
            { "type": "boolean" },
            { "type": "string", "const": "inline" },
            { "type": "string", "const": "hidden" }
          ],
          "description": "Whether to generate source map files.\n\nNote that this option will always be `true` if you have  {@link  https://www.typescriptlang.org/tsconfig/#declarationMap `declarationMap` }  option enabled in your `tsconfig.json`.",
          "markdownDescription": "Whether to generate source map files.\n\nNote that this option will always be `true` if you have\n {@link  https://www.typescriptlang.org/tsconfig/#declarationMap `declarationMap` } \noption enabled in your `tsconfig.json`.",
          "default": false
        },
        "clean": {
          "anyOf": [
            { "type": "boolean" },
            { "type": "array", "items": { "type": "string" } }
          ],
          "description": "Clean directories before build.\n\nDefault to output directory.",
          "markdownDescription": "Clean directories before build.\n\nDefault to output directory.",
          "default": true
        },
        "minify": {
          "anyOf": [
            { "type": "boolean" },
            { "type": "string", "const": "dce-only" },
            {
              "type": "object",
              "properties": {
                "compress": {
                  "anyOf": [
                    { "type": "boolean" },
                    {
                      "type": "object",
                      "properties": {
                        "target": {
                          "anyOf": [
                            { "type": "string" },
                            { "type": "array", "items": { "type": "string" } }
                          ],
                          "description": "Set desired EcmaScript standard version for output.\n\nSet `esnext` to enable all target highering.\n\nExample:\n\n* `'es2015'`\n* `['es2020', 'chrome58', 'edge16', 'firefox57', 'node12', 'safari11']`",
                          "markdownDescription": "Set desired EcmaScript standard version for output.\n\nSet `esnext` to enable all target highering.\n\nExample:\n\n* `'es2015'`\n* `['es2020', 'chrome58', 'edge16', 'firefox57', 'node12', 'safari11']`",
                          "default": "esnext"
                        },
                        "dropConsole": {
                          "type": "boolean",
                          "description": "Pass true to discard calls to `console.*`.",
                          "markdownDescription": "Pass true to discard calls to `console.*`.",
                          "default": false
                        },
                        "dropDebugger": {
                          "type": "boolean",
                          "description": "Remove `debugger;` statements.",
                          "markdownDescription": "Remove `debugger;` statements.",
                          "default": true
                        },
                        "unused": {
                          "anyOf": [
                            { "type": "boolean" },
                            { "type": "string", "const": "keep_assign" }
                          ],
                          "description": "Pass `true` to drop unreferenced functions and variables.\n\nSimple direct variable assignments do not count as references unless set to `keep_assign`.",
                          "markdownDescription": "Pass `true` to drop unreferenced functions and variables.\n\nSimple direct variable assignments do not count as references unless set to `keep_assign`.",
                          "default": true
                        },
                        "keepNames": {
                          "type": "object",
                          "properties": {
                            "function": {
                              "type": "boolean",
                              "description": "Keep function names so that `Function.prototype.name` is preserved.\n\nThis does not guarantee that the `undefined` name is preserved.",
                              "markdownDescription": "Keep function names so that `Function.prototype.name` is preserved.\n\nThis does not guarantee that the `undefined` name is preserved.",
                              "default": false
                            },
                            "class": {
                              "type": "boolean",
                              "description": "Keep class names so that `Class.prototype.name` is preserved.\n\nThis does not guarantee that the `undefined` name is preserved.",
                              "markdownDescription": "Keep class names so that `Class.prototype.name` is preserved.\n\nThis does not guarantee that the `undefined` name is preserved.",
                              "default": false
                            }
                          },
                          "required": ["function", "class"],
                          "additionalProperties": false,
                          "description": "Keep function / class names.",
                          "markdownDescription": "Keep function / class names."
                        },
                        "joinVars": {
                          "type": "boolean",
                          "description": "Join consecutive var, let and const statements.",
                          "markdownDescription": "Join consecutive var, let and const statements.",
                          "default": true
                        },
                        "sequences": {
                          "type": "boolean",
                          "description": "Join consecutive simple statements using the comma operator.\n\n`a; b` -> `a, b`",
                          "markdownDescription": "Join consecutive simple statements using the comma operator.\n\n`a; b` -> `a, b`",
                          "default": true
                        },
                        "dropLabels": {
                          "type": "array",
                          "items": { "type": "string" },
                          "description": "Set of label names to drop from the code.\n\nLabeled statements matching these names will be removed during minification.",
                          "markdownDescription": "Set of label names to drop from the code.\n\nLabeled statements matching these names will be removed during minification.",
                          "default": []
                        },
                        "maxIterations": {
                          "type": "number",
                          "description": "Limit the maximum number of iterations for debugging purpose.",
                          "markdownDescription": "Limit the maximum number of iterations for debugging purpose."
                        },
                        "treeshake": {
                          "type": "object",
                          "properties": {
                            "annotations": {
                              "type": "boolean",
                              "description": "Whether to respect the pure annotations.\n\nPure annotations are comments that mark an expression as pure. For example:",
                              "markdownDescription": "Whether to respect the pure annotations.\n\nPure annotations are comments that mark an expression as pure.\nFor example:",
                              "default": true
                            },
                            "manualPureFunctions": {
                              "type": "array",
                              "items": { "type": "string" },
                              "description": "Whether to treat this function call as pure.\n\nThis function is called for normal function calls, new calls, and tagged template calls.",
                              "markdownDescription": "Whether to treat this function call as pure.\n\nThis function is called for normal function calls, new calls, and\ntagged template calls."
                            },
                            "propertyReadSideEffects": {
                              "anyOf": [
                                { "type": "boolean" },
                                { "type": "string", "const": "always" }
                              ],
                              "description": "Whether property read accesses have side effects.",
                              "markdownDescription": "Whether property read accesses have side effects.",
                              "default": "always"
                            },
                            "propertyWriteSideEffects": {
                              "type": "boolean",
                              "description": "Whether property write accesses (assignments to member expressions) have side effects.\n\nWhen false, assignments like `obj.prop = value` are considered side-effect-free (assuming the object and value expressions themselves are side-effect-free).",
                              "markdownDescription": "Whether property write accesses (assignments to member expressions) have side effects.\n\nWhen false, assignments like `obj.prop = value` are considered side-effect-free\n(assuming the object and value expressions themselves are side-effect-free).",
                              "default": true
                            },
                            "unknownGlobalSideEffects": {
                              "type": "boolean",
                              "description": "Whether accessing a global variable has side effects.\n\nAccessing a non-existing global variable will throw an error. Global variable may be a getter that has side effects.",
                              "markdownDescription": "Whether accessing a global variable has side effects.\n\nAccessing a non-existing global variable will throw an error.\nGlobal variable may be a getter that has side effects.",
                              "default": true
                            },
                            "invalidImportSideEffects": {
                              "type": "boolean",
                              "description": "Whether invalid import statements have side effects.\n\nAccessing a non-existing import name will throw an error. Also import statements that cannot be resolved will throw an error.",
                              "markdownDescription": "Whether invalid import statements have side effects.\n\nAccessing a non-existing import name will throw an error.\nAlso import statements that cannot be resolved will throw an error.",
                              "default": true
                            }
                          },
                          "additionalProperties": false,
                          "description": "Treeshake options.",
                          "markdownDescription": "Treeshake options."
                        }
                      },
                      "additionalProperties": false
                    }
                  ]
                },
                "mangle": {
                  "anyOf": [
                    { "type": "boolean" },
                    {
                      "type": "object",
                      "properties": {
                        "toplevel": {
                          "type": "boolean",
                          "description": "Pass `true` to mangle names declared in the top level scope.",
                          "markdownDescription": "Pass `true` to mangle names declared in the top level scope.",
                          "default": "true for modules and commonjs, otherwise false"
                        },
                        "keepNames": {
                          "anyOf": [
                            { "type": "boolean" },
                            {
                              "type": "object",
                              "properties": {
                                "function": {
                                  "type": "boolean",
                                  "description": "Preserve `name` property for functions.",
                                  "markdownDescription": "Preserve `name` property for functions.",
                                  "default": false
                                },
                                "class": {
                                  "type": "boolean",
                                  "description": "Preserve `name` property for classes.",
                                  "markdownDescription": "Preserve `name` property for classes.",
                                  "default": false
                                }
                              },
                              "required": ["function", "class"],
                              "additionalProperties": false
                            }
                          ],
                          "description": "Preserve `name` property for functions and classes.",
                          "markdownDescription": "Preserve `name` property for functions and classes.",
                          "default": false
                        },
                        "debug": {
                          "type": "boolean",
                          "description": "Debug mangled names.",
                          "markdownDescription": "Debug mangled names."
                        }
                      },
                      "additionalProperties": false
                    }
                  ]
                },
                "codegen": {
                  "anyOf": [
                    { "type": "boolean" },
                    {
                      "type": "object",
                      "properties": {
                        "removeWhitespace": {
                          "type": "boolean",
                          "description": "Remove whitespace.",
                          "markdownDescription": "Remove whitespace.",
                          "default": true
                        }
                      },
                      "additionalProperties": false
                    }
                  ]
                }
              },
              "additionalProperties": false
            }
          ],
          "default": false
        },
        "footer": { "$ref": "#/definitions/ChunkAddon" },
        "banner": { "$ref": "#/definitions/ChunkAddon" },
        "unbundle": {
          "type": "boolean",
          "description": "Determines whether `unbundle` is enabled. When set to `true`, the output files will mirror the input file structure.",
          "markdownDescription": "Determines whether `unbundle` is enabled.\nWhen set to `true`, the output files will mirror the input file structure.",
          "default": false
        },
        "root": {
          "type": "string",
          "description": "Specifies the root directory of input files, similar to TypeScript's `rootDir`. This determines the output directory structure.\n\nBy default, the root is computed as the common base directory of all entry files.",
          "markdownDescription": "Specifies the root directory of input files, similar to TypeScript's `rootDir`.\nThis determines the output directory structure.\n\nBy default, the root is computed as the common base directory of all entry files."
        },
        "bundle": {
          "type": "boolean",
          "deprecated": "Use {@linkcode unbundle } instead.",
          "default": true
        },
        "fixedExtension": {
          "type": "boolean",
          "description": "Use a fixed extension for output files. The extension will always be `.cjs` or `.mjs`. Otherwise, it will depend on the package type.\n\nDefaults to `true` if  {@linkcode  platform }  is set to `node`, `false` otherwise.",
          "markdownDescription": "Use a fixed extension for output files.\nThe extension will always be `.cjs` or `.mjs`.\nOtherwise, it will depend on the package type.\n\nDefaults to `true` if  {@linkcode  platform }  is set to `node`,\n`false` otherwise.",
          "default": "platform === 'node'"
        },
        "hash": {
          "type": "boolean",
          "description": "If enabled, appends hash to chunk filenames.",
          "markdownDescription": "If enabled, appends hash to chunk filenames.",
          "default": true
        },
        "cjsDefault": { "type": "boolean", "default": true },
        "cwd": {
          "type": "string",
          "description": "The working directory of the config file.\n- Defaults to  {@linkcode  process.cwd process.cwd() }  for root config.\n- Defaults to the package directory for  {@linkcode  workspace }  config.",
          "markdownDescription": "The working directory of the config file.\n- Defaults to  {@linkcode  process.cwd process.cwd() }  for root config.\n- Defaults to the package directory for  {@linkcode  workspace }  config.",
          "default": "process.cwd()"
        },
        "name": {
          "type": "string",
          "description": "The name to show in CLI output. This is useful for monorepos or workspaces. When using workspace mode, this option defaults to the package name from package.json. In non-workspace mode, this option must be set explicitly for the name to show in the CLI output.",
          "markdownDescription": "The name to show in CLI output. This is useful for monorepos or workspaces.\nWhen using workspace mode, this option defaults to the package name from package.json.\nIn non-workspace mode, this option must be set explicitly for the name to show in the CLI output."
        },
        "logLevel": {
          "$ref": "#/definitions/LogLevel",
          "description": "Log level.",
          "markdownDescription": "Log level.",
          "default": "info"
        },
        "failOnWarn": {
          "anyOf": [
            { "type": "boolean" },
            { "$ref": "#/definitions/CIOption" }
          ],
          "description": "If true, fails the build on warnings.",
          "markdownDescription": "If true, fails the build on warnings.",
          "default": false
        },
        "fromVite": {
          "anyOf": [
            { "type": "boolean" },
            { "type": "string", "const": "vitest" }
          ],
          "description": "Reuse config from Vite or Vitest (experimental)",
          "markdownDescription": "Reuse config from Vite or Vitest (experimental)",
          "default": false
        },
        "watch": {
          "anyOf": [
            { "type": "boolean" },
            { "$ref": "#/definitions/Arrayable%3Cstring%3E" }
          ],
          "default": false
        },
        "ignoreWatch": {
          "$ref": "#/definitions/Arrayable%3C(string%7Cstring479082557)%3E",
          "description": "Files or patterns to not watch while in watch mode.",
          "markdownDescription": "Files or patterns to not watch while in watch mode."
        },
        "devtools": {
          "$ref": "#/definitions/WithEnabled%3CDevtoolsOptions%3E",
          "description": "**[experimental]** Enable devtools.\n\nDevTools is still under development, and this is for early testers only.\n\nThis may slow down the build process significantly.",
          "markdownDescription": "**[experimental]** Enable devtools.\n\nDevTools is still under development, and this is for early testers only.\n\nThis may slow down the build process significantly.",
          "default": false
        },
        "onSuccess": {
          "type": "string",
          "description": "You can specify command to be executed after a successful build, specially useful for Watch mode",
          "markdownDescription": "You can specify command to be executed after a successful build, specially useful for Watch mode"
        },
        "dts": {
          "$ref": "#/definitions/WithEnabled%3CDtsOptions%3E",
          "description": "Enables generation of TypeScript declaration files (`.d.ts`).\n\nBy default, this option is auto-detected based on your project's `package.json`:\n- If  {@linkcode  exe }  is enabled, declaration file generation is disabled by default.\n- If the `types` field is present, or if the main `exports` contains a `types` entry, declaration file generation is enabled by default.\n- Otherwise, declaration file generation is disabled by default.",
          "markdownDescription": "Enables generation of TypeScript declaration files (`.d.ts`).\n\nBy default, this option is auto-detected based on your project's `package.json`:\n- If  {@linkcode  exe }  is enabled, declaration file generation is disabled by default.\n- If the `types` field is present, or if the main `exports` contains a `types` entry, declaration file generation is enabled by default.\n- Otherwise, declaration file generation is disabled by default."
        },
        "unused": {
          "$ref": "#/definitions/WithEnabled%3Cinterface-715603908-146-604-715603908-0-884%3E",
          "description": "Enable unused dependencies check with `unplugin-unused` Requires `unplugin-unused` to be installed.",
          "markdownDescription": "Enable unused dependencies check with `unplugin-unused`\nRequires `unplugin-unused` to be installed.",
          "default": false
        },
        "publint": {
          "$ref": "#/definitions/WithEnabled%3CPublintOptions%3E",
          "description": "Run `publint` after bundling. Requires `publint` to be installed.",
          "markdownDescription": "Run `publint` after bundling.\nRequires `publint` to be installed.",
          "default": false
        },
        "attw": {
          "$ref": "#/definitions/WithEnabled%3CAttwOptions%3E",
          "description": "Run `arethetypeswrong` after bundling. Requires `@arethetypeswrong/core` to be installed.",
          "markdownDescription": "Run `arethetypeswrong` after bundling.\nRequires `@arethetypeswrong/core` to be installed.",
          "default": false
        },
        "report": {
          "$ref": "#/definitions/WithEnabled%3CReportOptions%3E",
          "description": "Enable size reporting after bundling.",
          "markdownDescription": "Enable size reporting after bundling.",
          "default": true
        },
        "globImport": {
          "type": "boolean",
          "description": "`import.meta.glob` support.",
          "markdownDescription": "`import.meta.glob` support.",
          "default": true
        },
        "exports": {
          "$ref": "#/definitions/WithEnabled%3CExportsOptions%3E",
          "description": "Generate package exports for `package.json`.\n\nThis will set the `main`, `module`, `types`, `exports` fields in `package.json` to point to the generated files.",
          "markdownDescription": "Generate package exports for `package.json`.\n\nThis will set the `main`, `module`, `types`, `exports` fields in `package.json`\nto point to the generated files.",
          "default": false
        },
        "injectStyle": {
          "type": "boolean",
          "deprecated": "Use {@linkcode CssOptions.inject css.inject} instead."
        },
        "publicDir": {
          "$ref": "#/definitions/CopyOptions",
          "deprecated": "Alias for {@linkcode copy }, will be removed in the future."
        },
        "copy": {
          "$ref": "#/definitions/CopyOptions",
          "description": "Copy files to another directory.",
          "markdownDescription": "Copy files to another directory."
        },
        "exe": {
          "$ref": "#/definitions/WithEnabled%3CExeOptions%3E",
          "description": "**[experimental]** Bundle as executable using Node.js SEA (Single Executable Applications).\n\nThis will bundle the output into a single executable file using Node.js SEA. Note that this is only supported on Node.js 25.7.0 and later, and is not supported in Bun or Deno.",
          "markdownDescription": "**[experimental]** Bundle as executable using Node.js SEA (Single Executable Applications).\n\nThis will bundle the output into a single executable file using Node.js SEA.\nNote that this is only supported on Node.js 25.7.0 and later, and is not supported in Bun or Deno.",
          "default": false
        },
        "workspace": {
          "anyOf": [
            { "$ref": "#/definitions/Workspace" },
            { "$ref": "#/definitions/Arrayable%3Cstring%3E" },
            { "type": "boolean", "const": true }
          ],
          "description": "**[experimental]** Enable workspace mode. This allows you to build multiple packages in a monorepo.",
          "markdownDescription": "**[experimental]** Enable workspace mode.\nThis allows you to build multiple packages in a monorepo."
        }
      },
      "additionalProperties": false,
      "description": "Options for tsdown.",
      "markdownDescription": "Options for tsdown."
    },
    "TsdownInputOption": {
      "$ref": "#/definitions/Arrayable%3C(string%7Calias-923471658-74155-74281-923471658-0-218972%3Cstring%2Cdef-alias-src_utils_types.ts-2028-2063-src_utils_types.ts-0-2064%3Cstring%3E%3E)%3E",
      "description": "Extended input option that supports glob negation patterns.\n\nWhen using object form, values can be:\n- A single glob pattern string\n- An array of glob patterns, including negation patterns (prefixed with `!`)",
      "markdownDescription": "Extended input option that supports glob negation patterns.\n\nWhen using object form, values can be:\n- A single glob pattern string\n- An array of glob patterns, including negation patterns (prefixed with `!`)"
    },
    "Arrayable<(string|alias-923471658-74155-74281-923471658-0-218972<string,def-alias-src_utils_types.ts-2028-2063-src_utils_types.ts-0-2064<string>>)>": {
      "anyOf": [
        { "type": "string" },
        {
          "type": "object",
          "additionalProperties": {
            "$ref": "#/definitions/Arrayable%3Cstring%3E"
          }
        },
        {
          "type": "array",
          "items": {
            "anyOf": [
              { "type": "string" },
              {
                "type": "object",
                "additionalProperties": {
                  "$ref": "#/definitions/Arrayable%3Cstring%3E"
                }
              }
            ]
          }
        }
      ]
    },
    "Arrayable<string>": {
      "anyOf": [
        { "type": "string" },
        { "type": "array", "items": { "type": "string" } }
      ]
    },
    "DepsConfig": {
      "type": "object",
      "properties": {
        "neverBundle": {
          "anyOf": [
            { "type": "string" },
            { "type": "string", "format": "regex" },
            {
              "type": "array",
              "items": {
                "anyOf": [
                  { "type": "string" },
                  { "type": "string", "format": "regex" }
                ]
              }
            }
          ],
          "description": "Mark dependencies as external (not bundled). Accepts strings, regular expressions, or Rolldown's  {@linkcode  ExternalOption } .",
          "markdownDescription": "Mark dependencies as external (not bundled).\nAccepts strings, regular expressions, or Rolldown's\n {@linkcode  ExternalOption } ."
        },
        "alwaysBundle": {
          "$ref": "#/definitions/Arrayable%3C(string%7Cstring479082557)%3E",
          "description": "Force dependencies to be bundled, even if they are in `dependencies`, `peerDependencies`, or `optionalDependencies`.",
          "markdownDescription": "Force dependencies to be bundled, even if they are in `dependencies`, `peerDependencies`, or `optionalDependencies`."
        },
        "onlyBundle": {
          "anyOf": [
            {
              "$ref": "#/definitions/Arrayable%3C(string%7Cstring479082557)%3E"
            },
            { "type": "boolean", "const": false }
          ],
          "description": "Whitelist of dependencies allowed to be bundled from `node_modules`. Throws an error if any unlisted dependency is bundled.\n\n- `undefined` (default): Show warnings for bundled dependencies.\n- `false`: Suppress all warnings about bundled dependencies.\n\nNote: Be sure to include all required sub-dependencies as well.",
          "markdownDescription": "Whitelist of dependencies allowed to be bundled from `node_modules`.\nThrows an error if any unlisted dependency is bundled.\n\n- `undefined` (default): Show warnings for bundled dependencies.\n- `false`: Suppress all warnings about bundled dependencies.\n\nNote: Be sure to include all required sub-dependencies as well."
        },
        "onlyAllowBundle": {
          "anyOf": [
            {
              "$ref": "#/definitions/Arrayable%3C(string%7Cstring479082557)%3E"
            },
            { "type": "boolean", "const": false }
          ],
          "deprecated": "Use {@linkcode onlyBundle } instead."
        },
        "skipNodeModulesBundle": {
          "type": "boolean",
          "description": "Skip bundling all `node_modules` dependencies.\n\n**Note:** This option cannot be used together with  {@linkcode  alwaysBundle } .",
          "markdownDescription": "Skip bundling all `node_modules` dependencies.\n\n**Note:** This option cannot be used together with  {@linkcode  alwaysBundle } .",
          "default": false
        }
      },
      "additionalProperties": false
    },
    "Arrayable<(string|string479082557)>": {
      "anyOf": [
        { "type": "string" },
        { "type": "string", "format": "regex" },
        {
          "type": "array",
          "items": {
            "anyOf": [
              { "type": "string" },
              { "type": "string", "format": "regex" }
            ]
          }
        }
      ]
    },
    "Arrayable<Format>": {
      "anyOf": [
        { "$ref": "#/definitions/Format" },
        { "type": "array", "items": { "$ref": "#/definitions/Format" } }
      ]
    },
    "Format": {
      "type": "string",
      "enum": ["es", "cjs", "esm", "module", "commonjs", "iife", "umd"]
    },
    "NormalizedFormat": {
      "type": "string",
      "enum": ["es", "cjs", "iife", "umd"],
      "description": "A normalized version of  {@linkcode  ModuleFormat } .",
      "markdownDescription": "A normalized version of  {@linkcode  ModuleFormat } ."
    },
    "PackageJsonWithPath": {
      "type": "object",
      "properties": { "packageJsonPath": { "type": "string" } },
      "required": ["packageJsonPath"]
    },
    "ResolvedDepsConfig": {
      "type": "object",
      "properties": {
        "neverBundle": {
          "anyOf": [
            { "type": "string" },
            { "type": "string", "format": "regex" },
            {
              "type": "array",
              "items": {
                "anyOf": [
                  { "type": "string" },
                  { "type": "string", "format": "regex" }
                ]
              }
            }
          ]
        },
        "onlyBundle": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "anyOf": [
                  { "type": "string" },
                  { "type": "string", "format": "regex" }
                ]
              }
            },
            { "type": "boolean", "const": false }
          ]
        },
        "skipNodeModulesBundle": { "type": "boolean" }
      },
      "required": ["skipNodeModulesBundle"],
      "additionalProperties": false
    },
    "DtsOptions": {
      "type": "object",
      "properties": {
        "build": {
          "type": "boolean",
          "description": "Build mode for the TypeScript compiler:\n\n- If `true`, the plugin will use [`tsc -b`](https://www.typescriptlang.org/docs/handbook/project-references.html#build-mode-for-typescript) to build the project and all referenced projects before emitting `.d.ts` files.\n- If `false`, the plugin will use [`tsc`](https://www.typescriptlang.org/docs/handbook/compiler-options.html) to emit `.d.ts` files without building referenced projects.",
          "markdownDescription": "Build mode for the TypeScript compiler:\n\n- If `true`, the plugin will use [`tsc -b`](https://www.typescriptlang.org/docs/handbook/project-references.html#build-mode-for-typescript) to build the project and all referenced projects before emitting `.d.ts` files.\n- If `false`, the plugin will use [`tsc`](https://www.typescriptlang.org/docs/handbook/compiler-options.html) to emit `.d.ts` files without building referenced projects.",
          "default": false
        },
        "incremental": {
          "type": "boolean",
          "description": "If your tsconfig.json has [`references`](https://www.typescriptlang.org/tsconfig/#references) option, `rolldown-plugin-dts` will use [`tsc\n-b`](https://www.typescriptlang.org/docs/handbook/project-references.html#build-mode-for-typescript) to build the project and all referenced projects before emitting `.d.ts` files.\n\nIn such case, if this option is `true`, `rolldown-plugin-dts` will write down all built files into your disk, including [`.tsbuildinfo`](https://www.typescriptlang.org/tsconfig/#tsBuildInfoFile) and other built files. This is equivalent to running `tsc -b` in your project.\n\nOtherwise, if this option is `false`, `rolldown-plugin-dts` will write built files only into memory and leave a small footprint in your disk.\n\nEnabling this option will decrease the build time by caching previous build results. This is helpful when you have a large project with multiple referenced projects.\n\nBy default, `incremental` is `true` if your tsconfig has [`incremental`](https://www.typescriptlang.org/tsconfig/#incremental) or [`tsBuildInfoFile`](https://www.typescriptlang.org/tsconfig/#tsBuildInfoFile) enabled.\n\nThis option is only used when  {@link  Options.oxc }  is `false`.",
          "markdownDescription": "If your tsconfig.json has\n[`references`](https://www.typescriptlang.org/tsconfig/#references) option,\n`rolldown-plugin-dts` will use [`tsc\n-b`](https://www.typescriptlang.org/docs/handbook/project-references.html#build-mode-for-typescript)\nto build the project and all referenced projects before emitting `.d.ts`\nfiles.\n\nIn such case, if this option is `true`, `rolldown-plugin-dts` will write\ndown all built files into your disk, including\n[`.tsbuildinfo`](https://www.typescriptlang.org/tsconfig/#tsBuildInfoFile)\nand other built files. This is equivalent to running `tsc -b` in your\nproject.\n\nOtherwise, if this option is `false`, `rolldown-plugin-dts` will write\nbuilt files only into memory and leave a small footprint in your disk.\n\nEnabling this option will decrease the build time by caching previous build\nresults. This is helpful when you have a large project with multiple\nreferenced projects.\n\nBy default, `incremental` is `true` if your tsconfig has\n[`incremental`](https://www.typescriptlang.org/tsconfig/#incremental) or\n[`tsBuildInfoFile`](https://www.typescriptlang.org/tsconfig/#tsBuildInfoFile)\nenabled.\n\nThis option is only used when  {@link  Options.oxc }  is\n`false`."
        },
        "vue": {
          "type": "boolean",
          "description": "If `true`, the plugin will generate `.d.ts` files using `vue-tsc`.",
          "markdownDescription": "If `true`, the plugin will generate `.d.ts` files using `vue-tsc`."
        },
        "tsMacro": {
          "type": "boolean",
          "description": "If `true`, the plugin will generate `.d.ts` files using `@ts-macro/tsc`.",
          "markdownDescription": "If `true`, the plugin will generate `.d.ts` files using `@ts-macro/tsc`."
        },
        "parallel": {
          "type": "boolean",
          "description": "If `true`, the plugin will launch a separate process for `tsc` or `vue-tsc`. This enables processing multiple projects in parallel.",
          "markdownDescription": "If `true`, the plugin will launch a separate process for `tsc` or `vue-tsc`.\nThis enables processing multiple projects in parallel."
        },
        "eager": {
          "type": "boolean",
          "description": "If `true`, the plugin will prepare all files listed in `tsconfig.json` for `tsc` or `vue-tsc`.\n\nThis is especially useful when you have a single `tsconfig.json` for multiple projects in a monorepo.",
          "markdownDescription": "If `true`, the plugin will prepare all files listed in `tsconfig.json` for `tsc` or `vue-tsc`.\n\nThis is especially useful when you have a single `tsconfig.json` for multiple projects in a monorepo."
        },
        "newContext": {
          "type": "boolean",
          "description": "If `true`, the plugin will create a new isolated context for each build, ensuring that previously generated `.d.ts` code and caches are not reused.\n\nBy default, the plugin may reuse internal caches or incremental build artifacts to speed up repeated builds. Enabling this option forces a clean context, guaranteeing that all type definitions are generated from scratch.",
          "markdownDescription": "If `true`, the plugin will create a new isolated context for each build,\nensuring that previously generated `.d.ts` code and caches are not reused.\n\nBy default, the plugin may reuse internal caches or incremental build artifacts\nto speed up repeated builds. Enabling this option forces a clean context,\nguaranteeing that all type definitions are generated from scratch.",
          "default": false
        },
        "emitJs": {
          "type": "boolean",
          "description": "If `true`, the plugin will emit `.d.ts` files for `.js` files as well. This is useful when you want to generate type definitions for JavaScript files with JSDoc comments.\n\nEnabled by default when `allowJs` in compilerOptions is `true`. This option is only used when  {@link  Options.oxc }  is `false`.",
          "markdownDescription": "If `true`, the plugin will emit `.d.ts` files for `.js` files as well.\nThis is useful when you want to generate type definitions for JavaScript files with JSDoc comments.\n\nEnabled by default when `allowJs` in compilerOptions is `true`.\nThis option is only used when  {@link  Options.oxc }  is\n`false`."
        },
        "entry": {
          "anyOf": [
            { "type": "string" },
            { "type": "array", "items": { "type": "string" } }
          ],
          "description": "Glob pattern(s) to filter which entry files get `.d.ts` generation.\n\nWhen specified, only entry files matching these patterns will emit `.d.ts` chunks. When not specified, all entries get `.d.ts` generation.\n\nSupports negation patterns (e.g., `['**', '!src/icons/**']`) for exclusion. Patterns are matched against file paths relative to `cwd`.",
          "markdownDescription": "Glob pattern(s) to filter which entry files get `.d.ts` generation.\n\nWhen specified, only entry files matching these patterns will emit `.d.ts` chunks.\nWhen not specified, all entries get `.d.ts` generation.\n\nSupports negation patterns (e.g., `['**', '!src/icons/**']`) for exclusion.\nPatterns are matched against file paths relative to `cwd`."
        },
        "cwd": {
          "type": "string",
          "description": "The directory in which the plugin will search for the `tsconfig.json` file.",
          "markdownDescription": "The directory in which the plugin will search for the `tsconfig.json` file."
        },
        "dtsInput": {
          "type": "boolean",
          "description": "Set to `true` if your entry files are `.d.ts` files instead of `.ts` files.\n\nWhen enabled, the plugin will skip generating a `.d.ts` file for the entry point.",
          "markdownDescription": "Set to `true` if your entry files are `.d.ts` files instead of `.ts` files.\n\nWhen enabled, the plugin will skip generating a `.d.ts` file for the entry point."
        },
        "emitDtsOnly": {
          "type": "boolean",
          "description": "If `true`, the plugin will emit only `.d.ts` files and remove all other output chunks.\n\nThis is especially useful when generating `.d.ts` files for the CommonJS format as part of a separate build step.",
          "markdownDescription": "If `true`, the plugin will emit only `.d.ts` files and remove all other output chunks.\n\nThis is especially useful when generating `.d.ts` files for the CommonJS format as part of a separate build step."
        },
        "tsconfig": {
          "type": ["string", "boolean"],
          "description": "The path to the `tsconfig.json` file.\n\nIf set to `false`, the plugin will ignore any `tsconfig.json` file. You can still specify `compilerOptions` directly in the options.",
          "markdownDescription": "The path to the `tsconfig.json` file.\n\nIf set to `false`, the plugin will ignore any `tsconfig.json` file.\nYou can still specify `compilerOptions` directly in the options.",
          "default": "tsconfig.json"
        },
        "tsconfigRaw": {
          "type": "object",
          "properties": {
            "watchOptions": {
              "$ref": "#/definitions/TsConfigJson.WatchOptions",
              "description": "Instructs the TypeScript compiler how to watch files.",
              "markdownDescription": "Instructs the TypeScript compiler how to watch files."
            },
            "typeAcquisition": {
              "$ref": "#/definitions/TsConfigJson.TypeAcquisition",
              "description": "Auto type (.d.ts) acquisition options for this project.",
              "markdownDescription": "Auto type (.d.ts) acquisition options for this project."
            },
            "compileOnSave": {
              "type": "boolean",
              "description": "Enable Compile-on-Save for this project.",
              "markdownDescription": "Enable Compile-on-Save for this project."
            },
            "extends": {
              "anyOf": [
                { "type": "string" },
                { "type": "array", "items": { "type": "string" } }
              ],
              "description": "Path to base configuration file to inherit from.",
              "markdownDescription": "Path to base configuration file to inherit from."
            },
            "files": {
              "type": "array",
              "items": { "type": "string" },
              "description": "If no `files` or `include` property is present in a `tsconfig.json`, the compiler defaults to including all files in the containing directory and subdirectories except those specified by `exclude`. When a `files` property is specified, only those files and those specified by `include` are included.",
              "markdownDescription": "If no `files` or `include` property is present in a `tsconfig.json`, the compiler defaults to including all files in the containing directory and subdirectories except those specified by `exclude`. When a `files` property is specified, only those files and those specified by `include` are included."
            },
            "exclude": {
              "type": "array",
              "items": { "type": "string" },
              "description": "Specifies a list of files to be excluded from compilation. The `exclude` property only affects the files included via the `include` property and not the `files` property.\n\nGlob patterns require TypeScript version 2.0 or later.",
              "markdownDescription": "Specifies a list of files to be excluded from compilation. The `exclude` property only affects the files included via the `include` property and not the `files` property.\n\nGlob patterns require TypeScript version 2.0 or later."
            },
            "include": {
              "type": "array",
              "items": { "type": "string" },
              "description": "Specifies a list of glob patterns that match files to be included in compilation.\n\nIf no `files` or `include` property is present in a `tsconfig.json`, the compiler defaults to including all files in the containing directory and subdirectories except those specified by `exclude`.",
              "markdownDescription": "Specifies a list of glob patterns that match files to be included in compilation.\n\nIf no `files` or `include` property is present in a `tsconfig.json`, the compiler defaults to including all files in the containing directory and subdirectories except those specified by `exclude`."
            },
            "references": {
              "type": "array",
              "items": { "$ref": "#/definitions/TsConfigJson.References" },
              "description": "Referenced projects.",
              "markdownDescription": "Referenced projects."
            }
          },
          "additionalProperties": false,
          "description": "Pass a raw `tsconfig.json` object directly to the plugin.",
          "markdownDescription": "Pass a raw `tsconfig.json` object directly to the plugin."
        },
        "compilerOptions": {
          "$ref": "#/definitions/TsConfigJson.CompilerOptions",
          "description": "Override the `compilerOptions` specified in `tsconfig.json`.",
          "markdownDescription": "Override the `compilerOptions` specified in `tsconfig.json`."
        },
        "sourcemap": {
          "type": "boolean",
          "description": "If `true`, the plugin will generate declaration maps (`.d.ts.map`) for `.d.ts` files.",
          "markdownDescription": "If `true`, the plugin will generate declaration maps (`.d.ts.map`) for `.d.ts` files."
        },
        "resolver": {
          "type": "string",
          "enum": ["oxc", "tsc"],
          "description": "Specifies a resolver to resolve type definitions, especially for `node_modules`.\n\n- `'oxc'`: Uses Oxc's module resolution, which is faster and more efficient.\n- `'tsc'`: Uses TypeScript's native module resolution, which may be more compatible with complex setups, but slower.",
          "markdownDescription": "Specifies a resolver to resolve type definitions, especially for `node_modules`.\n\n- `'oxc'`: Uses Oxc's module resolution, which is faster and more efficient.\n- `'tsc'`: Uses TypeScript's native module resolution, which may be more compatible with complex setups, but slower.",
          "default": "oxc"
        },
        "cjsDefault": {
          "type": "boolean",
          "description": "Determines how the default export is emitted.\n\nIf set to `true`, and you are only exporting a single item using `export default ...`, the output will use `export = ...` instead of the standard ES module syntax. This is useful for compatibility with CommonJS.",
          "markdownDescription": "Determines how the default export is emitted.\n\nIf set to `true`, and you are only exporting a single item using `export default ...`,\nthe output will use `export = ...` instead of the standard ES module syntax.\nThis is useful for compatibility with CommonJS."
        },
        "sideEffects": {
          "type": "boolean",
          "description": "Indicates whether the generated `.d.ts` files have side effects.\n- If set to `true`, Rolldown will treat the `.d.ts` files as having side effects during tree-shaking.\n- If set to `false`, Rolldown may consider the `.d.ts` files as side-effect-free, potentially removing them if they are not imported.",
          "markdownDescription": "Indicates whether the generated `.d.ts` files have side effects.\n- If set to `true`, Rolldown will treat the `.d.ts` files as having side effects during tree-shaking.\n- If set to `false`, Rolldown may consider the `.d.ts` files as side-effect-free, potentially removing them if they are not imported.",
          "default": false
        },
        "oxc": {
          "anyOf": [
            { "type": "boolean" },
            {
              "type": "object",
              "properties": {
                "stripInternal": {
                  "type": "boolean",
                  "description": "Do not emit declarations for code that has an",
                  "markdownDescription": "Do not emit declarations for code that has an"
                }
              },
              "additionalProperties": false
            }
          ],
          "description": "If `true`, the plugin will generate `.d.ts` files using Oxc, which is significantly faster than the TypeScript compiler.\n\nThis option is automatically enabled when `isolatedDeclarations` in `compilerOptions` is set to `true`.",
          "markdownDescription": "If `true`, the plugin will generate `.d.ts` files using Oxc,\nwhich is significantly faster than the TypeScript compiler.\n\nThis option is automatically enabled when `isolatedDeclarations` in `compilerOptions` is set to `true`."
        },
        "tsgo": {
          "anyOf": [
            { "type": "boolean" },
            {
              "type": "object",
              "properties": {
                "enabled": { "type": "boolean" },
                "path": {
                  "type": "string",
                  "description": "Custom path to the `tsgo` binary.",
                  "markdownDescription": "Custom path to the `tsgo` binary."
                }
              },
              "additionalProperties": false
            }
          ],
          "description": "**[Experimental]** Enables DTS generation using `tsgo`.\n\nTo use this option, make sure `@typescript/native-preview` is installed as a dependency, or provide a custom path to the `tsgo` binary using the `path` option.\n\n**Note:** This option is not yet recommended for production environments. `tsconfigRaw` and `isolatedDeclarations` options will be ignored when this option is enabled.\n\n\n```ts // Use tsgo from `@typescript/native-preview` dependency tsgo: true\n\n// Use custom tsgo path (e.g., managed by Nix) tsgo: { path: '/path/to/tsgo' } ```",
          "markdownDescription": "**[Experimental]** Enables DTS generation using `tsgo`.\n\nTo use this option, make sure `@typescript/native-preview` is installed as a dependency,\nor provide a custom path to the `tsgo` binary using the `path` option.\n\n**Note:** This option is not yet recommended for production environments.\n`tsconfigRaw` and `isolatedDeclarations` options will be ignored when this option is enabled.\n\n\n```ts\n// Use tsgo from `@typescript/native-preview` dependency\ntsgo: true\n\n// Use custom tsgo path (e.g., managed by Nix)\ntsgo: { path: '/path/to/tsgo' }\n```"
        },
        "cjsReexport": {
          "type": "boolean",
          "description": "When building dual ESM+CJS formats, generate a `.d.cts` re-export stub instead of running a full second TypeScript compilation pass.\n\nThe stub re-exports everything from the corresponding `.d.mts` file, ensuring CJS and ESM consumers share the same type declarations. This eliminates the TypeScript \"dual module hazard\" where separate `.d.cts` and `.d.mts` declarations cause `TS2352` (\"neither type sufficiently overlaps\") errors when casting between types derived from the same class.\n\nOnly applies when building both `esm` and `cjs` formats simultaneously.",
          "markdownDescription": "When building dual ESM+CJS formats, generate a `.d.cts` re-export stub\ninstead of running a full second TypeScript compilation pass.\n\nThe stub re-exports everything from the corresponding `.d.mts` file,\nensuring CJS and ESM consumers share the same type declarations. This\neliminates the TypeScript \"dual module hazard\" where separate `.d.cts`\nand `.d.mts` declarations cause `TS2352` (\"neither type sufficiently\noverlaps\") errors when casting between types derived from the same class.\n\nOnly applies when building both `esm` and `cjs` formats simultaneously.",
          "default": false
        }
      },
      "additionalProperties": false
    },
    "TsConfigJson.WatchOptions": {
      "$ref": "https://www.schemastore.org/tsconfig.json#/definitions/watchOptionsDefinition/properties/watchOptions"
    },
    "TsConfigJson.TypeAcquisition": {
      "$ref": "https://www.schemastore.org/tsconfig.json#/definitions/typeAcquisitionDefinition/properties/typeAcquisition"
    },
    "TsConfigJson.References": {
      "$ref": "https://www.schemastore.org/tsconfig.json#/definitions/referencesDefinition/properties/references"
    },
    "TsConfigJson.CompilerOptions": {
      "$ref": "https://www.schemastore.org/tsconfig.json#/definitions/compilerOptionsDefinition/properties/compilerOptions"
    },
    "ReportOptions": {
      "type": "object",
      "properties": {
        "gzip": {
          "type": "boolean",
          "description": "Enable/disable gzip-compressed size reporting. Compressing large output files can be slow, so disabling this may increase build performance for large projects.",
          "markdownDescription": "Enable/disable gzip-compressed size reporting.\nCompressing large output files can be slow, so disabling this may increase build performance for large projects.",
          "default": true
        },
        "brotli": {
          "type": "boolean",
          "description": "Enable/disable brotli-compressed size reporting. Compressing large output files can be slow, so disabling this may increase build performance for large projects.",
          "markdownDescription": "Enable/disable brotli-compressed size reporting.\nCompressing large output files can be slow, so disabling this may increase build performance for large projects.",
          "default": false
        },
        "maxCompressSize": {
          "type": "number",
          "description": "Skip reporting compressed size for files larger than this size. Defaults to `1_000_000` (1 MB).",
          "markdownDescription": "Skip reporting compressed size for files larger than this size.\nDefaults to `1_000_000` (1 MB).",
          "default": 1000000
        }
      },
      "additionalProperties": false
    },
    "ExportsOptions": {
      "type": "object",
      "properties": {
        "devExports": {
          "type": ["boolean", "string"],
          "description": "Generate exports that link to source code during development.\n- `string`: add as a custom condition.\n- `true`: all conditions point to source files, and add `dist` exports to `publishConfig`.",
          "markdownDescription": "Generate exports that link to source code during development.\n- `string`: add as a custom condition.\n- `true`: all conditions point to source files, and add `dist` exports to `publishConfig`."
        },
        "packageJson": {
          "type": "boolean",
          "description": "Generate `exports` for `package.json` file.",
          "markdownDescription": "Generate `exports` for `package.json` file.",
          "default": true
        },
        "all": {
          "type": "boolean",
          "description": "Generate `exports` for all files.",
          "markdownDescription": "Generate `exports` for all files.",
          "default": false
        },
        "exclude": {
          "type": "array",
          "items": {
            "anyOf": [
              { "type": "string", "format": "regex" },
              { "type": "string" }
            ]
          },
          "description": "Specifies file patterns (as glob patterns or regular expressions) to exclude from package exports. Use this to prevent certain files from being included in the exported package, such as test files, binaries, or internal utilities.\n\n**Note:** Do not include file extensions, and paths should be relative to the dist directory.",
          "markdownDescription": "Specifies file patterns (as glob patterns or regular expressions) to exclude from package exports.\nUse this to prevent certain files from being included in the exported package, such as test files, binaries, or internal utilities.\n\n**Note:** Do not include file extensions, and paths should be relative to the dist directory."
        },
        "legacy": {
          "type": "boolean",
          "description": "Generate legacy fields (`main` and `module`) for older Node.js and bundlers that do not support package `exports` field.\n\nDefaults to false, if only ESM builds are included, true otherwise.",
          "markdownDescription": "Generate legacy fields (`main` and `module`) for older Node.js and bundlers\nthat do not support package `exports` field.\n\nDefaults to false, if only ESM builds are included, true otherwise."
        },
        "customExports": {
          "type": "object",
          "description": "Specifies custom exports to add to the package exports in addition to the ones generated by tsdown. Use this to add additional exports in the exported package, such as workers or assets.",
          "markdownDescription": "Specifies custom exports to add to the package exports in addition to the ones generated by tsdown.\nUse this to add additional exports in the exported package, such as workers or assets."
        },
        "inlinedDependencies": {
          "type": "boolean",
          "description": "Generate `inlinedDependencies` field in `package.json`. Lists dependencies that are physically inlined into the bundle with their exact versions.",
          "markdownDescription": "Generate `inlinedDependencies` field in `package.json`.\nLists dependencies that are physically inlined into the bundle with their exact versions.",
          "default": true
        },
        "extensions": {
          "type": "boolean",
          "description": "Add file extensions to subpath export keys.\n\nWhen enabled, all subpath exports (except the root `\".\"`) will include a `.js` extension in the key (e.g., `\"./utils.js\"` instead of `\"./utils\"`).\n\nThis follows the Node.js recommendation for subpath exports:",
          "markdownDescription": "Add file extensions to subpath export keys.\n\nWhen enabled, all subpath exports (except the root `\".\"`) will include\na `.js` extension in the key (e.g., `\"./utils.js\"` instead of `\"./utils\"`).\n\nThis follows the Node.js recommendation for subpath exports:",
          "default": false
        },
        "bin": {
          "anyOf": [
            { "type": "boolean" },
            { "type": "string" },
            { "type": "object", "additionalProperties": { "type": "string" } }
          ],
          "description": "Generate the `bin` field in `package.json` for CLI executables.\n\nControls how command names are mapped to built entry files:\n\n- `true`: Auto-detect a single CLI entry from entry chunks that contain   a shebang (for example, `#!/usr/bin/env node`). The command name is   derived from the package name without its scope. Throws if multiple   shebang entries are found. Warns and skips generation if none are found.\n- `string`: Use the given source file path (relative to `cwd`) as the   CLI entry. The command name is derived from the package name without   its scope. Warns if the source file does not contain a shebang.\n- `Record<string, string>`: Explicitly map command names to source file   paths (relative to `cwd`). Warns for each source file that does not   contain a shebang.\n\nWhen  {@link  ExportsOptions.devExports }  is enabled, the `bin` field in `package.json` points to source files during local development, while `publishConfig.bin` points to built output paths for publishing.",
          "markdownDescription": "Generate the `bin` field in `package.json` for CLI executables.\n\nControls how command names are mapped to built entry files:\n\n- `true`: Auto-detect a single CLI entry from entry chunks that contain\n  a shebang (for example, `#!/usr/bin/env node`). The command name is\n  derived from the package name without its scope. Throws if multiple\n  shebang entries are found. Warns and skips generation if none are found.\n- `string`: Use the given source file path (relative to `cwd`) as the\n  CLI entry. The command name is derived from the package name without\n  its scope. Warns if the source file does not contain a shebang.\n- `Record<string, string>`: Explicitly map command names to source file\n  paths (relative to `cwd`). Warns for each source file that does not\n  contain a shebang.\n\nWhen  {@link  ExportsOptions.devExports }  is enabled, the `bin` field in\n`package.json` points to source files during local development, while\n`publishConfig.bin` points to built output paths for publishing."
        }
      },
      "additionalProperties": false
    },
    "DevtoolsOptions": {
      "type": "object",
      "properties": {
        "ui": {
          "anyOf": [
            { "type": "boolean" },
            {
              "type": "object",
              "properties": {
                "root": { "type": "string" },
                "config": { "type": "string" },
                "host": { "type": "string" },
                "port": { "type": ["string", "number"] },
                "open": { "type": "boolean" }
              },
              "additionalProperties": false
            }
          ],
          "description": "**[experimental]** Enable devtools integration. `@vitejs/devtools` must be installed as a dependency.\n\nDefaults to true, if `@vitejs/devtools` is installed.",
          "markdownDescription": "**[experimental]** Enable devtools integration. `@vitejs/devtools` must be installed as a dependency.\n\nDefaults to true, if `@vitejs/devtools` is installed."
        },
        "clean": {
          "type": "boolean",
          "description": "Clean devtools stale sessions.",
          "markdownDescription": "Clean devtools stale sessions.",
          "default": true
        }
      }
    },
    "PublintOptions": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "level": {
          "type": "string",
          "enum": ["suggestion", "warning", "error"],
          "description": "The level of messages to log (default: `'suggestion'`).\n- `suggestion`: logs all messages\n- `warning`: logs only `warning` and `error` messages\n- `error`: logs only `error` messages",
          "markdownDescription": "The level of messages to log (default: `'suggestion'`).\n- `suggestion`: logs all messages\n- `warning`: logs only `warning` and `error` messages\n- `error`: logs only `error` messages"
        },
        "strict": {
          "type": "boolean",
          "description": "Report warnings as errors. This runs before `level` filters the result, which means that if `level` is set to `'error'`, all warnings (elevated as errors) will still be reported.",
          "markdownDescription": "Report warnings as errors. This runs before `level` filters the result, which means that if\n`level` is set to `'error'`, all warnings (elevated as errors) will still be reported."
        }
      }
    },
    "AttwOptions": {
      "type": "object",
      "properties": {
        "entrypoints": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Exhaustive list of entrypoints to check. The package root is `\".\"`. Specifying this option disables automatic entrypoint discovery, and overrides the `includeEntrypoints` and `excludeEntrypoints` options.",
          "markdownDescription": "Exhaustive list of entrypoints to check. The package root is `\".\"`.\nSpecifying this option disables automatic entrypoint discovery,\nand overrides the `includeEntrypoints` and `excludeEntrypoints` options."
        },
        "includeEntrypoints": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Entrypoints to check in addition to automatically discovered ones.",
          "markdownDescription": "Entrypoints to check in addition to automatically discovered ones."
        },
        "excludeEntrypoints": {
          "type": "array",
          "items": {
            "anyOf": [
              { "type": "string" },
              { "type": "string", "format": "regex" }
            ]
          },
          "description": "Entrypoints to exclude from checking.",
          "markdownDescription": "Entrypoints to exclude from checking."
        },
        "entrypointsLegacy": {
          "type": "boolean",
          "description": "Whether to automatically consider all published files as entrypoints in the absence of any other detected or configured entrypoints.",
          "markdownDescription": "Whether to automatically consider all published files as entrypoints\nin the absence of any other detected or configured entrypoints."
        },
        "profile": {
          "type": "string",
          "enum": ["strict", "node16", "esm-only"],
          "description": "Profiles select a set of resolution modes to require/ignore. All are evaluated but failures outside of those required are ignored.\n\nThe available profiles are:\n- `strict`: requires all resolutions\n- `node16`: ignores node10 resolution failures\n- `esm-only`: ignores CJS resolution failures",
          "markdownDescription": "Profiles select a set of resolution modes to require/ignore. All are evaluated but failures outside\nof those required are ignored.\n\nThe available profiles are:\n- `strict`: requires all resolutions\n- `node16`: ignores node10 resolution failures\n- `esm-only`: ignores CJS resolution failures",
          "default": "strict"
        },
        "level": {
          "type": "string",
          "enum": ["error", "warn"],
          "description": "The level of the check.\n\nThe available levels are:\n- `error`: fails the build\n- `warn`: warns the build",
          "markdownDescription": "The level of the check.\n\nThe available levels are:\n- `error`: fails the build\n- `warn`: warns the build",
          "default": "warn"
        },
        "ignoreRules": {
          "type": "array",
          "items": {
            "anyOf": [
              { "type": "string" },
              {
                "type": "string",
                "enum": [
                  "no-resolution",
                  "untyped-resolution",
                  "false-cjs",
                  "false-esm",
                  "cjs-resolves-to-esm",
                  "fallback-condition",
                  "cjs-only-exports-default",
                  "named-exports",
                  "false-export-default",
                  "missing-export-equals",
                  "unexpected-module-syntax",
                  "internal-resolution-error"
                ]
              }
            ]
          },
          "description": "List of problem types to ignore by rule name.\n\nThe available values are:\n- `no-resolution`\n- `untyped-resolution`\n- `false-cjs`\n- `false-esm`\n- `cjs-resolves-to-esm`\n- `fallback-condition`\n- `cjs-only-exports-default`\n- `named-exports`\n- `false-export-default`\n- `missing-export-equals`\n- `unexpected-module-syntax`\n- `internal-resolution-error`",
          "markdownDescription": "List of problem types to ignore by rule name.\n\nThe available values are:\n- `no-resolution`\n- `untyped-resolution`\n- `false-cjs`\n- `false-esm`\n- `cjs-resolves-to-esm`\n- `fallback-condition`\n- `cjs-only-exports-default`\n- `named-exports`\n- `false-export-default`\n- `missing-export-equals`\n- `unexpected-module-syntax`\n- `internal-resolution-error`",
          "default": [],
          "uniqueItems": true
        }
      },
      "additionalProperties": false
    },
    "ExeOptions": {
      "type": "object",
      "properties": {
        "targets": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "platform": {
                "type": "string",
                "enum": ["win", "darwin", "linux"]
              },
              "arch": { "type": "string", "enum": ["x64", "arm64"] },
              "nodeVersion": {
                "type": "string",
                "description": "Node.js version to use for the executable.\n\nAccepts a valid semver string (e.g., `\"25.7.0\"`), or the special values `\"latest\"` / `\"latest-lts\"` which resolve the version automatically from  {@link  https://nodejs.org/dist/index.json } .\n\nThe minimum required version is 25.7.0, which is when SEA support was added to Node.js.",
                "markdownDescription": "Node.js version to use for the executable.\n\nAccepts a valid semver string (e.g., `\"25.7.0\"`), or the special values\n`\"latest\"` / `\"latest-lts\"` which resolve the version automatically from\n {@link  https://nodejs.org/dist/index.json } .\n\nThe minimum required version is 25.7.0, which is when SEA support was added to Node.js."
              }
            },
            "required": ["platform", "arch", "nodeVersion"],
            "additionalProperties": false
          },
          "description": "Cross-platform targets for building executables. Requires `@tsdown/exe` to be installed. When specified, builds an executable for each target platform/arch combination.",
          "markdownDescription": "Cross-platform targets for building executables.\nRequires `@tsdown/exe` to be installed.\nWhen specified, builds an executable for each target platform/arch combination."
        },
        "seaConfig": {
          "type": "object",
          "properties": {
            "executable": {
              "type": "string",
              "description": "Optional, if not specified, uses the current Node.js binary",
              "markdownDescription": "Optional, if not specified, uses the current Node.js binary"
            },
            "disableExperimentalSEAWarning": {
              "type": "boolean",
              "default": true
            },
            "useSnapshot": { "type": "boolean", "default": false },
            "useCodeCache": { "type": "boolean", "default": false },
            "execArgv": { "type": "array", "items": { "type": "string" } },
            "execArgvExtension": {
              "type": "string",
              "enum": ["none", "env", "cli"],
              "default": "env"
            },
            "assets": {
              "type": "object",
              "additionalProperties": { "type": "string" }
            }
          },
          "additionalProperties": false
        },
        "fileName": {
          "type": "string",
          "description": "Output file name without any suffix or extension. For example, do not include `.exe`, platform suffixes, or architecture suffixes.",
          "markdownDescription": "Output file name without any suffix or extension.\nFor example, do not include `.exe`, platform suffixes, or architecture suffixes."
        },
        "outDir": {
          "type": "string",
          "description": "Output directory for executables.",
          "markdownDescription": "Output directory for executables.",
          "default": "build"
        }
      },
      "additionalProperties": false
    },
    "ChunkAddon": {
      "anyOf": [
        { "$ref": "#/definitions/ChunkAddonObject" },
        { "type": "string" }
      ]
    },
    "ChunkAddonObject": {
      "type": "object",
      "properties": {
        "js": { "type": "string" },
        "css": { "type": "string" },
        "dts": { "type": "string" }
      },
      "additionalProperties": false
    },
    "LogLevel": {
      "anyOf": [
        { "$ref": "#/definitions/LogType" },
        { "type": "string", "const": "silent" }
      ]
    },
    "LogType": { "type": "string", "enum": ["error", "warn", "info"] },
    "CIOption": { "type": "string", "enum": ["ci-only", "local-only"] },
    "WithEnabled<DevtoolsOptions>": {
      "anyOf": [
        { "type": "boolean" },
        { "$ref": "#/definitions/CIOption" },
        {
          "type": "object",
          "properties": {
            "enabled": {
              "anyOf": [
                { "type": "boolean" },
                { "$ref": "#/definitions/CIOption" }
              ],
              "default": true
            },
            "ui": {
              "anyOf": [
                { "type": "boolean" },
                {
                  "type": "object",
                  "properties": {
                    "root": { "type": "string" },
                    "config": { "type": "string" },
                    "host": { "type": "string" },
                    "port": { "type": ["string", "number"] },
                    "open": { "type": "boolean" }
                  },
                  "additionalProperties": false
                }
              ],
              "description": "**[experimental]** Enable devtools integration. `@vitejs/devtools` must be installed as a dependency.\n\nDefaults to true, if `@vitejs/devtools` is installed.",
              "markdownDescription": "**[experimental]** Enable devtools integration. `@vitejs/devtools` must be installed as a dependency.\n\nDefaults to true, if `@vitejs/devtools` is installed."
            },
            "clean": {
              "type": "boolean",
              "description": "Clean devtools stale sessions.",
              "markdownDescription": "Clean devtools stale sessions.",
              "default": true
            }
          }
        }
      ]
    },
    "WithEnabled<DtsOptions>": {
      "anyOf": [
        { "type": "boolean" },
        { "$ref": "#/definitions/CIOption" },
        {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "enabled": {
              "anyOf": [
                { "type": "boolean" },
                { "$ref": "#/definitions/CIOption" }
              ],
              "default": true
            },
            "build": {
              "type": "boolean",
              "description": "Build mode for the TypeScript compiler:\n\n- If `true`, the plugin will use [`tsc -b`](https://www.typescriptlang.org/docs/handbook/project-references.html#build-mode-for-typescript) to build the project and all referenced projects before emitting `.d.ts` files.\n- If `false`, the plugin will use [`tsc`](https://www.typescriptlang.org/docs/handbook/compiler-options.html) to emit `.d.ts` files without building referenced projects.",
              "markdownDescription": "Build mode for the TypeScript compiler:\n\n- If `true`, the plugin will use [`tsc -b`](https://www.typescriptlang.org/docs/handbook/project-references.html#build-mode-for-typescript) to build the project and all referenced projects before emitting `.d.ts` files.\n- If `false`, the plugin will use [`tsc`](https://www.typescriptlang.org/docs/handbook/compiler-options.html) to emit `.d.ts` files without building referenced projects.",
              "default": false
            },
            "incremental": {
              "type": "boolean",
              "description": "If your tsconfig.json has [`references`](https://www.typescriptlang.org/tsconfig/#references) option, `rolldown-plugin-dts` will use [`tsc\n-b`](https://www.typescriptlang.org/docs/handbook/project-references.html#build-mode-for-typescript) to build the project and all referenced projects before emitting `.d.ts` files.\n\nIn such case, if this option is `true`, `rolldown-plugin-dts` will write down all built files into your disk, including [`.tsbuildinfo`](https://www.typescriptlang.org/tsconfig/#tsBuildInfoFile) and other built files. This is equivalent to running `tsc -b` in your project.\n\nOtherwise, if this option is `false`, `rolldown-plugin-dts` will write built files only into memory and leave a small footprint in your disk.\n\nEnabling this option will decrease the build time by caching previous build results. This is helpful when you have a large project with multiple referenced projects.\n\nBy default, `incremental` is `true` if your tsconfig has [`incremental`](https://www.typescriptlang.org/tsconfig/#incremental) or [`tsBuildInfoFile`](https://www.typescriptlang.org/tsconfig/#tsBuildInfoFile) enabled.\n\nThis option is only used when  {@link  Options.oxc }  is `false`.",
              "markdownDescription": "If your tsconfig.json has\n[`references`](https://www.typescriptlang.org/tsconfig/#references) option,\n`rolldown-plugin-dts` will use [`tsc\n-b`](https://www.typescriptlang.org/docs/handbook/project-references.html#build-mode-for-typescript)\nto build the project and all referenced projects before emitting `.d.ts`\nfiles.\n\nIn such case, if this option is `true`, `rolldown-plugin-dts` will write\ndown all built files into your disk, including\n[`.tsbuildinfo`](https://www.typescriptlang.org/tsconfig/#tsBuildInfoFile)\nand other built files. This is equivalent to running `tsc -b` in your\nproject.\n\nOtherwise, if this option is `false`, `rolldown-plugin-dts` will write\nbuilt files only into memory and leave a small footprint in your disk.\n\nEnabling this option will decrease the build time by caching previous build\nresults. This is helpful when you have a large project with multiple\nreferenced projects.\n\nBy default, `incremental` is `true` if your tsconfig has\n[`incremental`](https://www.typescriptlang.org/tsconfig/#incremental) or\n[`tsBuildInfoFile`](https://www.typescriptlang.org/tsconfig/#tsBuildInfoFile)\nenabled.\n\nThis option is only used when  {@link  Options.oxc }  is\n`false`."
            },
            "vue": {
              "type": "boolean",
              "description": "If `true`, the plugin will generate `.d.ts` files using `vue-tsc`.",
              "markdownDescription": "If `true`, the plugin will generate `.d.ts` files using `vue-tsc`."
            },
            "tsMacro": {
              "type": "boolean",
              "description": "If `true`, the plugin will generate `.d.ts` files using `@ts-macro/tsc`.",
              "markdownDescription": "If `true`, the plugin will generate `.d.ts` files using `@ts-macro/tsc`."
            },
            "parallel": {
              "type": "boolean",
              "description": "If `true`, the plugin will launch a separate process for `tsc` or `vue-tsc`. This enables processing multiple projects in parallel.",
              "markdownDescription": "If `true`, the plugin will launch a separate process for `tsc` or `vue-tsc`.\nThis enables processing multiple projects in parallel."
            },
            "eager": {
              "type": "boolean",
              "description": "If `true`, the plugin will prepare all files listed in `tsconfig.json` for `tsc` or `vue-tsc`.\n\nThis is especially useful when you have a single `tsconfig.json` for multiple projects in a monorepo.",
              "markdownDescription": "If `true`, the plugin will prepare all files listed in `tsconfig.json` for `tsc` or `vue-tsc`.\n\nThis is especially useful when you have a single `tsconfig.json` for multiple projects in a monorepo."
            },
            "newContext": {
              "type": "boolean",
              "description": "If `true`, the plugin will create a new isolated context for each build, ensuring that previously generated `.d.ts` code and caches are not reused.\n\nBy default, the plugin may reuse internal caches or incremental build artifacts to speed up repeated builds. Enabling this option forces a clean context, guaranteeing that all type definitions are generated from scratch.",
              "markdownDescription": "If `true`, the plugin will create a new isolated context for each build,\nensuring that previously generated `.d.ts` code and caches are not reused.\n\nBy default, the plugin may reuse internal caches or incremental build artifacts\nto speed up repeated builds. Enabling this option forces a clean context,\nguaranteeing that all type definitions are generated from scratch.",
              "default": false
            },
            "emitJs": {
              "type": "boolean",
              "description": "If `true`, the plugin will emit `.d.ts` files for `.js` files as well. This is useful when you want to generate type definitions for JavaScript files with JSDoc comments.\n\nEnabled by default when `allowJs` in compilerOptions is `true`. This option is only used when  {@link  Options.oxc }  is `false`.",
              "markdownDescription": "If `true`, the plugin will emit `.d.ts` files for `.js` files as well.\nThis is useful when you want to generate type definitions for JavaScript files with JSDoc comments.\n\nEnabled by default when `allowJs` in compilerOptions is `true`.\nThis option is only used when  {@link  Options.oxc }  is\n`false`."
            },
            "entry": {
              "anyOf": [
                { "type": "string" },
                { "type": "array", "items": { "type": "string" } }
              ],
              "description": "Glob pattern(s) to filter which entry files get `.d.ts` generation.\n\nWhen specified, only entry files matching these patterns will emit `.d.ts` chunks. When not specified, all entries get `.d.ts` generation.\n\nSupports negation patterns (e.g., `['**', '!src/icons/**']`) for exclusion. Patterns are matched against file paths relative to `cwd`.",
              "markdownDescription": "Glob pattern(s) to filter which entry files get `.d.ts` generation.\n\nWhen specified, only entry files matching these patterns will emit `.d.ts` chunks.\nWhen not specified, all entries get `.d.ts` generation.\n\nSupports negation patterns (e.g., `['**', '!src/icons/**']`) for exclusion.\nPatterns are matched against file paths relative to `cwd`."
            },
            "cwd": {
              "type": "string",
              "description": "The directory in which the plugin will search for the `tsconfig.json` file.",
              "markdownDescription": "The directory in which the plugin will search for the `tsconfig.json` file."
            },
            "dtsInput": {
              "type": "boolean",
              "description": "Set to `true` if your entry files are `.d.ts` files instead of `.ts` files.\n\nWhen enabled, the plugin will skip generating a `.d.ts` file for the entry point.",
              "markdownDescription": "Set to `true` if your entry files are `.d.ts` files instead of `.ts` files.\n\nWhen enabled, the plugin will skip generating a `.d.ts` file for the entry point."
            },
            "emitDtsOnly": {
              "type": "boolean",
              "description": "If `true`, the plugin will emit only `.d.ts` files and remove all other output chunks.\n\nThis is especially useful when generating `.d.ts` files for the CommonJS format as part of a separate build step.",
              "markdownDescription": "If `true`, the plugin will emit only `.d.ts` files and remove all other output chunks.\n\nThis is especially useful when generating `.d.ts` files for the CommonJS format as part of a separate build step."
            },
            "tsconfig": {
              "type": ["string", "boolean"],
              "description": "The path to the `tsconfig.json` file.\n\nIf set to `false`, the plugin will ignore any `tsconfig.json` file. You can still specify `compilerOptions` directly in the options.",
              "markdownDescription": "The path to the `tsconfig.json` file.\n\nIf set to `false`, the plugin will ignore any `tsconfig.json` file.\nYou can still specify `compilerOptions` directly in the options.",
              "default": "tsconfig.json"
            },
            "tsconfigRaw": {
              "type": "object",
              "properties": {
                "watchOptions": {
                  "$ref": "#/definitions/TsConfigJson.WatchOptions",
                  "description": "Instructs the TypeScript compiler how to watch files.",
                  "markdownDescription": "Instructs the TypeScript compiler how to watch files."
                },
                "typeAcquisition": {
                  "$ref": "#/definitions/TsConfigJson.TypeAcquisition",
                  "description": "Auto type (.d.ts) acquisition options for this project.",
                  "markdownDescription": "Auto type (.d.ts) acquisition options for this project."
                },
                "compileOnSave": {
                  "type": "boolean",
                  "description": "Enable Compile-on-Save for this project.",
                  "markdownDescription": "Enable Compile-on-Save for this project."
                },
                "extends": {
                  "anyOf": [
                    { "type": "string" },
                    { "type": "array", "items": { "type": "string" } }
                  ],
                  "description": "Path to base configuration file to inherit from.",
                  "markdownDescription": "Path to base configuration file to inherit from."
                },
                "files": {
                  "type": "array",
                  "items": { "type": "string" },
                  "description": "If no `files` or `include` property is present in a `tsconfig.json`, the compiler defaults to including all files in the containing directory and subdirectories except those specified by `exclude`. When a `files` property is specified, only those files and those specified by `include` are included.",
                  "markdownDescription": "If no `files` or `include` property is present in a `tsconfig.json`, the compiler defaults to including all files in the containing directory and subdirectories except those specified by `exclude`. When a `files` property is specified, only those files and those specified by `include` are included."
                },
                "exclude": {
                  "type": "array",
                  "items": { "type": "string" },
                  "description": "Specifies a list of files to be excluded from compilation. The `exclude` property only affects the files included via the `include` property and not the `files` property.\n\nGlob patterns require TypeScript version 2.0 or later.",
                  "markdownDescription": "Specifies a list of files to be excluded from compilation. The `exclude` property only affects the files included via the `include` property and not the `files` property.\n\nGlob patterns require TypeScript version 2.0 or later."
                },
                "include": {
                  "type": "array",
                  "items": { "type": "string" },
                  "description": "Specifies a list of glob patterns that match files to be included in compilation.\n\nIf no `files` or `include` property is present in a `tsconfig.json`, the compiler defaults to including all files in the containing directory and subdirectories except those specified by `exclude`.",
                  "markdownDescription": "Specifies a list of glob patterns that match files to be included in compilation.\n\nIf no `files` or `include` property is present in a `tsconfig.json`, the compiler defaults to including all files in the containing directory and subdirectories except those specified by `exclude`."
                },
                "references": {
                  "type": "array",
                  "items": { "$ref": "#/definitions/TsConfigJson.References" },
                  "description": "Referenced projects.",
                  "markdownDescription": "Referenced projects."
                }
              },
              "additionalProperties": false,
              "description": "Pass a raw `tsconfig.json` object directly to the plugin.",
              "markdownDescription": "Pass a raw `tsconfig.json` object directly to the plugin."
            },
            "compilerOptions": {
              "$ref": "#/definitions/TsConfigJson.CompilerOptions",
              "description": "Override the `compilerOptions` specified in `tsconfig.json`.",
              "markdownDescription": "Override the `compilerOptions` specified in `tsconfig.json`."
            },
            "sourcemap": {
              "type": "boolean",
              "description": "If `true`, the plugin will generate declaration maps (`.d.ts.map`) for `.d.ts` files.",
              "markdownDescription": "If `true`, the plugin will generate declaration maps (`.d.ts.map`) for `.d.ts` files."
            },
            "resolver": {
              "type": "string",
              "enum": ["oxc", "tsc"],
              "description": "Specifies a resolver to resolve type definitions, especially for `node_modules`.\n\n- `'oxc'`: Uses Oxc's module resolution, which is faster and more efficient.\n- `'tsc'`: Uses TypeScript's native module resolution, which may be more compatible with complex setups, but slower.",
              "markdownDescription": "Specifies a resolver to resolve type definitions, especially for `node_modules`.\n\n- `'oxc'`: Uses Oxc's module resolution, which is faster and more efficient.\n- `'tsc'`: Uses TypeScript's native module resolution, which may be more compatible with complex setups, but slower.",
              "default": "oxc"
            },
            "cjsDefault": {
              "type": "boolean",
              "description": "Determines how the default export is emitted.\n\nIf set to `true`, and you are only exporting a single item using `export default ...`, the output will use `export = ...` instead of the standard ES module syntax. This is useful for compatibility with CommonJS.",
              "markdownDescription": "Determines how the default export is emitted.\n\nIf set to `true`, and you are only exporting a single item using `export default ...`,\nthe output will use `export = ...` instead of the standard ES module syntax.\nThis is useful for compatibility with CommonJS."
            },
            "sideEffects": {
              "type": "boolean",
              "description": "Indicates whether the generated `.d.ts` files have side effects.\n- If set to `true`, Rolldown will treat the `.d.ts` files as having side effects during tree-shaking.\n- If set to `false`, Rolldown may consider the `.d.ts` files as side-effect-free, potentially removing them if they are not imported.",
              "markdownDescription": "Indicates whether the generated `.d.ts` files have side effects.\n- If set to `true`, Rolldown will treat the `.d.ts` files as having side effects during tree-shaking.\n- If set to `false`, Rolldown may consider the `.d.ts` files as side-effect-free, potentially removing them if they are not imported.",
              "default": false
            },
            "oxc": {
              "anyOf": [
                { "type": "boolean" },
                {
                  "type": "object",
                  "properties": {
                    "stripInternal": {
                      "type": "boolean",
                      "description": "Do not emit declarations for code that has an",
                      "markdownDescription": "Do not emit declarations for code that has an"
                    }
                  },
                  "additionalProperties": false
                }
              ],
              "description": "If `true`, the plugin will generate `.d.ts` files using Oxc, which is significantly faster than the TypeScript compiler.\n\nThis option is automatically enabled when `isolatedDeclarations` in `compilerOptions` is set to `true`.",
              "markdownDescription": "If `true`, the plugin will generate `.d.ts` files using Oxc,\nwhich is significantly faster than the TypeScript compiler.\n\nThis option is automatically enabled when `isolatedDeclarations` in `compilerOptions` is set to `true`."
            },
            "tsgo": {
              "anyOf": [
                { "type": "boolean" },
                {
                  "type": "object",
                  "properties": {
                    "enabled": { "type": "boolean" },
                    "path": {
                      "type": "string",
                      "description": "Custom path to the `tsgo` binary.",
                      "markdownDescription": "Custom path to the `tsgo` binary."
                    }
                  },
                  "additionalProperties": false
                }
              ],
              "description": "**[Experimental]** Enables DTS generation using `tsgo`.\n\nTo use this option, make sure `@typescript/native-preview` is installed as a dependency, or provide a custom path to the `tsgo` binary using the `path` option.\n\n**Note:** This option is not yet recommended for production environments. `tsconfigRaw` and `isolatedDeclarations` options will be ignored when this option is enabled.\n\n\n```ts // Use tsgo from `@typescript/native-preview` dependency tsgo: true\n\n// Use custom tsgo path (e.g., managed by Nix) tsgo: { path: '/path/to/tsgo' } ```",
              "markdownDescription": "**[Experimental]** Enables DTS generation using `tsgo`.\n\nTo use this option, make sure `@typescript/native-preview` is installed as a dependency,\nor provide a custom path to the `tsgo` binary using the `path` option.\n\n**Note:** This option is not yet recommended for production environments.\n`tsconfigRaw` and `isolatedDeclarations` options will be ignored when this option is enabled.\n\n\n```ts\n// Use tsgo from `@typescript/native-preview` dependency\ntsgo: true\n\n// Use custom tsgo path (e.g., managed by Nix)\ntsgo: { path: '/path/to/tsgo' }\n```"
            },
            "cjsReexport": {
              "type": "boolean",
              "description": "When building dual ESM+CJS formats, generate a `.d.cts` re-export stub instead of running a full second TypeScript compilation pass.\n\nThe stub re-exports everything from the corresponding `.d.mts` file, ensuring CJS and ESM consumers share the same type declarations. This eliminates the TypeScript \"dual module hazard\" where separate `.d.cts` and `.d.mts` declarations cause `TS2352` (\"neither type sufficiently overlaps\") errors when casting between types derived from the same class.\n\nOnly applies when building both `esm` and `cjs` formats simultaneously.",
              "markdownDescription": "When building dual ESM+CJS formats, generate a `.d.cts` re-export stub\ninstead of running a full second TypeScript compilation pass.\n\nThe stub re-exports everything from the corresponding `.d.mts` file,\nensuring CJS and ESM consumers share the same type declarations. This\neliminates the TypeScript \"dual module hazard\" where separate `.d.cts`\nand `.d.mts` declarations cause `TS2352` (\"neither type sufficiently\noverlaps\") errors when casting between types derived from the same class.\n\nOnly applies when building both `esm` and `cjs` formats simultaneously.",
              "default": false
            }
          }
        }
      ]
    },
    "WithEnabled<interface-715603908-146-604-715603908-0-884>": {
      "anyOf": [
        { "type": "boolean" },
        { "$ref": "#/definitions/CIOption" },
        {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "enabled": {
              "anyOf": [
                { "type": "boolean" },
                { "$ref": "#/definitions/CIOption" }
              ],
              "default": true
            },
            "root": { "type": "string" },
            "include": {
              "anyOf": [
                { "type": "string" },
                { "type": "string", "format": "regex" },
                {
                  "type": "array",
                  "items": {
                    "anyOf": [
                      { "type": "string" },
                      { "type": "string", "format": "regex" }
                    ]
                  }
                }
              ],
              "description": "Array, or not yet",
              "markdownDescription": "Array, or not yet"
            },
            "exclude": {
              "anyOf": [
                { "type": "string" },
                { "type": "string", "format": "regex" },
                {
                  "type": "array",
                  "items": {
                    "anyOf": [
                      { "type": "string" },
                      { "type": "string", "format": "regex" }
                    ]
                  }
                }
              ],
              "description": "Array, or not yet",
              "markdownDescription": "Array, or not yet"
            },
            "ignore": {
              "anyOf": [
                { "type": "array", "items": { "type": "string" } },
                {
                  "type": "object",
                  "properties": {
                    "dependencies": {
                      "type": "array",
                      "items": { "type": "string" }
                    },
                    "devDependencies": {
                      "type": "array",
                      "items": { "type": "string" }
                    },
                    "peerDependencies": {
                      "type": "array",
                      "items": { "type": "string" }
                    }
                  },
                  "additionalProperties": false
                }
              ]
            },
            "level": {
              "type": "string",
              "enum": ["warning", "error"],
              "description": "Specifies the severity level of the check.\n\n- `'error'`: Causes the build to fail.\n- `'warning'`: Displays a warning in the console.",
              "markdownDescription": "Specifies the severity level of the check.\n\n- `'error'`: Causes the build to fail.\n- `'warning'`: Displays a warning in the console.",
              "default": "warning"
            },
            "depKinds": {
              "type": "array",
              "items": {
                "type": "string",
                "enum": ["dependencies", "devDependencies", "peerDependencies"]
              },
              "default": ["dependencies", "peerDependencies"]
            }
          }
        }
      ]
    },
    "WithEnabled<PublintOptions>": {
      "anyOf": [
        { "type": "boolean" },
        { "$ref": "#/definitions/CIOption" },
        {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "enabled": {
              "anyOf": [
                { "type": "boolean" },
                { "$ref": "#/definitions/CIOption" }
              ],
              "default": true
            },
            "level": {
              "type": "string",
              "enum": ["suggestion", "warning", "error"],
              "description": "The level of messages to log (default: `'suggestion'`).\n- `suggestion`: logs all messages\n- `warning`: logs only `warning` and `error` messages\n- `error`: logs only `error` messages",
              "markdownDescription": "The level of messages to log (default: `'suggestion'`).\n- `suggestion`: logs all messages\n- `warning`: logs only `warning` and `error` messages\n- `error`: logs only `error` messages"
            },
            "strict": {
              "type": "boolean",
              "description": "Report warnings as errors. This runs before `level` filters the result, which means that if `level` is set to `'error'`, all warnings (elevated as errors) will still be reported.",
              "markdownDescription": "Report warnings as errors. This runs before `level` filters the result, which means that if\n`level` is set to `'error'`, all warnings (elevated as errors) will still be reported."
            }
          }
        }
      ]
    },
    "WithEnabled<AttwOptions>": {
      "anyOf": [
        { "type": "boolean" },
        { "$ref": "#/definitions/CIOption" },
        {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "enabled": {
              "anyOf": [
                { "type": "boolean" },
                { "$ref": "#/definitions/CIOption" }
              ],
              "default": true
            },
            "entrypoints": {
              "type": "array",
              "items": { "type": "string" },
              "description": "Exhaustive list of entrypoints to check. The package root is `\".\"`. Specifying this option disables automatic entrypoint discovery, and overrides the `includeEntrypoints` and `excludeEntrypoints` options.",
              "markdownDescription": "Exhaustive list of entrypoints to check. The package root is `\".\"`.\nSpecifying this option disables automatic entrypoint discovery,\nand overrides the `includeEntrypoints` and `excludeEntrypoints` options."
            },
            "includeEntrypoints": {
              "type": "array",
              "items": { "type": "string" },
              "description": "Entrypoints to check in addition to automatically discovered ones.",
              "markdownDescription": "Entrypoints to check in addition to automatically discovered ones."
            },
            "excludeEntrypoints": {
              "type": "array",
              "items": {
                "anyOf": [
                  { "type": "string" },
                  { "type": "string", "format": "regex" }
                ]
              },
              "description": "Entrypoints to exclude from checking.",
              "markdownDescription": "Entrypoints to exclude from checking."
            },
            "entrypointsLegacy": {
              "type": "boolean",
              "description": "Whether to automatically consider all published files as entrypoints in the absence of any other detected or configured entrypoints.",
              "markdownDescription": "Whether to automatically consider all published files as entrypoints\nin the absence of any other detected or configured entrypoints."
            },
            "profile": {
              "type": "string",
              "enum": ["strict", "node16", "esm-only"],
              "description": "Profiles select a set of resolution modes to require/ignore. All are evaluated but failures outside of those required are ignored.\n\nThe available profiles are:\n- `strict`: requires all resolutions\n- `node16`: ignores node10 resolution failures\n- `esm-only`: ignores CJS resolution failures",
              "markdownDescription": "Profiles select a set of resolution modes to require/ignore. All are evaluated but failures outside\nof those required are ignored.\n\nThe available profiles are:\n- `strict`: requires all resolutions\n- `node16`: ignores node10 resolution failures\n- `esm-only`: ignores CJS resolution failures",
              "default": "strict"
            },
            "level": {
              "type": "string",
              "enum": ["error", "warn"],
              "description": "The level of the check.\n\nThe available levels are:\n- `error`: fails the build\n- `warn`: warns the build",
              "markdownDescription": "The level of the check.\n\nThe available levels are:\n- `error`: fails the build\n- `warn`: warns the build",
              "default": "warn"
            },
            "ignoreRules": {
              "type": "array",
              "items": {
                "anyOf": [
                  { "type": "string" },
                  {
                    "type": "string",
                    "enum": [
                      "no-resolution",
                      "untyped-resolution",
                      "false-cjs",
                      "false-esm",
                      "cjs-resolves-to-esm",
                      "fallback-condition",
                      "cjs-only-exports-default",
                      "named-exports",
                      "false-export-default",
                      "missing-export-equals",
                      "unexpected-module-syntax",
                      "internal-resolution-error"
                    ]
                  }
                ]
              },
              "description": "List of problem types to ignore by rule name.\n\nThe available values are:\n- `no-resolution`\n- `untyped-resolution`\n- `false-cjs`\n- `false-esm`\n- `cjs-resolves-to-esm`\n- `fallback-condition`\n- `cjs-only-exports-default`\n- `named-exports`\n- `false-export-default`\n- `missing-export-equals`\n- `unexpected-module-syntax`\n- `internal-resolution-error`",
              "markdownDescription": "List of problem types to ignore by rule name.\n\nThe available values are:\n- `no-resolution`\n- `untyped-resolution`\n- `false-cjs`\n- `false-esm`\n- `cjs-resolves-to-esm`\n- `fallback-condition`\n- `cjs-only-exports-default`\n- `named-exports`\n- `false-export-default`\n- `missing-export-equals`\n- `unexpected-module-syntax`\n- `internal-resolution-error`",
              "default": [],
              "uniqueItems": true
            }
          }
        }
      ]
    },
    "WithEnabled<ReportOptions>": {
      "anyOf": [
        { "type": "boolean" },
        { "$ref": "#/definitions/CIOption" },
        {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "enabled": {
              "anyOf": [
                { "type": "boolean" },
                { "$ref": "#/definitions/CIOption" }
              ],
              "default": true
            },
            "gzip": {
              "type": "boolean",
              "description": "Enable/disable gzip-compressed size reporting. Compressing large output files can be slow, so disabling this may increase build performance for large projects.",
              "markdownDescription": "Enable/disable gzip-compressed size reporting.\nCompressing large output files can be slow, so disabling this may increase build performance for large projects.",
              "default": true
            },
            "brotli": {
              "type": "boolean",
              "description": "Enable/disable brotli-compressed size reporting. Compressing large output files can be slow, so disabling this may increase build performance for large projects.",
              "markdownDescription": "Enable/disable brotli-compressed size reporting.\nCompressing large output files can be slow, so disabling this may increase build performance for large projects.",
              "default": false
            },
            "maxCompressSize": {
              "type": "number",
              "description": "Skip reporting compressed size for files larger than this size. Defaults to `1_000_000` (1 MB).",
              "markdownDescription": "Skip reporting compressed size for files larger than this size.\nDefaults to `1_000_000` (1 MB).",
              "default": 1000000
            }
          }
        }
      ]
    },
    "WithEnabled<ExportsOptions>": {
      "anyOf": [
        { "type": "boolean" },
        { "$ref": "#/definitions/CIOption" },
        {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "enabled": {
              "anyOf": [
                { "type": "boolean" },
                { "$ref": "#/definitions/CIOption" }
              ],
              "default": true
            },
            "devExports": {
              "type": ["boolean", "string"],
              "description": "Generate exports that link to source code during development.\n- `string`: add as a custom condition.\n- `true`: all conditions point to source files, and add `dist` exports to `publishConfig`.",
              "markdownDescription": "Generate exports that link to source code during development.\n- `string`: add as a custom condition.\n- `true`: all conditions point to source files, and add `dist` exports to `publishConfig`."
            },
            "packageJson": {
              "type": "boolean",
              "description": "Generate `exports` for `package.json` file.",
              "markdownDescription": "Generate `exports` for `package.json` file.",
              "default": true
            },
            "all": {
              "type": "boolean",
              "description": "Generate `exports` for all files.",
              "markdownDescription": "Generate `exports` for all files.",
              "default": false
            },
            "exclude": {
              "type": "array",
              "items": {
                "anyOf": [
                  { "type": "string", "format": "regex" },
                  { "type": "string" }
                ]
              },
              "description": "Specifies file patterns (as glob patterns or regular expressions) to exclude from package exports. Use this to prevent certain files from being included in the exported package, such as test files, binaries, or internal utilities.\n\n**Note:** Do not include file extensions, and paths should be relative to the dist directory.",
              "markdownDescription": "Specifies file patterns (as glob patterns or regular expressions) to exclude from package exports.\nUse this to prevent certain files from being included in the exported package, such as test files, binaries, or internal utilities.\n\n**Note:** Do not include file extensions, and paths should be relative to the dist directory."
            },
            "legacy": {
              "type": "boolean",
              "description": "Generate legacy fields (`main` and `module`) for older Node.js and bundlers that do not support package `exports` field.\n\nDefaults to false, if only ESM builds are included, true otherwise.",
              "markdownDescription": "Generate legacy fields (`main` and `module`) for older Node.js and bundlers\nthat do not support package `exports` field.\n\nDefaults to false, if only ESM builds are included, true otherwise."
            },
            "customExports": {
              "type": "object",
              "description": "Specifies custom exports to add to the package exports in addition to the ones generated by tsdown. Use this to add additional exports in the exported package, such as workers or assets.",
              "markdownDescription": "Specifies custom exports to add to the package exports in addition to the ones generated by tsdown.\nUse this to add additional exports in the exported package, such as workers or assets."
            },
            "inlinedDependencies": {
              "type": "boolean",
              "description": "Generate `inlinedDependencies` field in `package.json`. Lists dependencies that are physically inlined into the bundle with their exact versions.",
              "markdownDescription": "Generate `inlinedDependencies` field in `package.json`.\nLists dependencies that are physically inlined into the bundle with their exact versions.",
              "default": true
            },
            "extensions": {
              "type": "boolean",
              "description": "Add file extensions to subpath export keys.\n\nWhen enabled, all subpath exports (except the root `\".\"`) will include a `.js` extension in the key (e.g., `\"./utils.js\"` instead of `\"./utils\"`).\n\nThis follows the Node.js recommendation for subpath exports:",
              "markdownDescription": "Add file extensions to subpath export keys.\n\nWhen enabled, all subpath exports (except the root `\".\"`) will include\na `.js` extension in the key (e.g., `\"./utils.js\"` instead of `\"./utils\"`).\n\nThis follows the Node.js recommendation for subpath exports:",
              "default": false
            },
            "bin": {
              "anyOf": [
                { "type": "boolean" },
                { "type": "string" },
                {
                  "type": "object",
                  "additionalProperties": { "type": "string" }
                }
              ],
              "description": "Generate the `bin` field in `package.json` for CLI executables.\n\nControls how command names are mapped to built entry files:\n\n- `true`: Auto-detect a single CLI entry from entry chunks that contain   a shebang (for example, `#!/usr/bin/env node`). The command name is   derived from the package name without its scope. Throws if multiple   shebang entries are found. Warns and skips generation if none are found.\n- `string`: Use the given source file path (relative to `cwd`) as the   CLI entry. The command name is derived from the package name without   its scope. Warns if the source file does not contain a shebang.\n- `Record<string, string>`: Explicitly map command names to source file   paths (relative to `cwd`). Warns for each source file that does not   contain a shebang.\n\nWhen  {@link  ExportsOptions.devExports }  is enabled, the `bin` field in `package.json` points to source files during local development, while `publishConfig.bin` points to built output paths for publishing.",
              "markdownDescription": "Generate the `bin` field in `package.json` for CLI executables.\n\nControls how command names are mapped to built entry files:\n\n- `true`: Auto-detect a single CLI entry from entry chunks that contain\n  a shebang (for example, `#!/usr/bin/env node`). The command name is\n  derived from the package name without its scope. Throws if multiple\n  shebang entries are found. Warns and skips generation if none are found.\n- `string`: Use the given source file path (relative to `cwd`) as the\n  CLI entry. The command name is derived from the package name without\n  its scope. Warns if the source file does not contain a shebang.\n- `Record<string, string>`: Explicitly map command names to source file\n  paths (relative to `cwd`). Warns for each source file that does not\n  contain a shebang.\n\nWhen  {@link  ExportsOptions.devExports }  is enabled, the `bin` field in\n`package.json` points to source files during local development, while\n`publishConfig.bin` points to built output paths for publishing."
            }
          }
        }
      ]
    },
    "CopyOptions": {
      "$ref": "#/definitions/Arrayable%3C(string%7CCopyEntry)%3E"
    },
    "Arrayable<(string|CopyEntry)>": {
      "anyOf": [
        { "type": "string" },
        { "$ref": "#/definitions/CopyEntry" },
        {
          "type": "array",
          "items": {
            "anyOf": [
              { "type": "string" },
              { "$ref": "#/definitions/CopyEntry" }
            ]
          }
        }
      ]
    },
    "CopyEntry": {
      "type": "object",
      "properties": {
        "from": {
          "$ref": "#/definitions/Arrayable%3Cstring%3E",
          "description": "Source path or glob pattern.",
          "markdownDescription": "Source path or glob pattern."
        },
        "to": {
          "type": "string",
          "description": "Destination path. If not specified, defaults to the output directory (\"outDir\").",
          "markdownDescription": "Destination path.\nIf not specified, defaults to the output directory (\"outDir\")."
        },
        "flatten": {
          "type": "boolean",
          "description": "Whether to flatten the copied files (not preserving directory structure).",
          "markdownDescription": "Whether to flatten the copied files (not preserving directory structure).",
          "default": true
        },
        "verbose": {
          "type": "boolean",
          "description": "Output copied items to console.",
          "markdownDescription": "Output copied items to console.",
          "default": false
        },
        "rename": {
          "type": "string",
          "description": "Change destination file or folder name.",
          "markdownDescription": "Change destination file or folder name."
        }
      },
      "required": ["from"],
      "additionalProperties": false
    },
    "WithEnabled<ExeOptions>": {
      "anyOf": [
        { "type": "boolean" },
        { "$ref": "#/definitions/CIOption" },
        {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "enabled": {
              "anyOf": [
                { "type": "boolean" },
                { "$ref": "#/definitions/CIOption" }
              ],
              "default": true
            },
            "targets": {
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "platform": {
                    "type": "string",
                    "enum": ["win", "darwin", "linux"]
                  },
                  "arch": { "type": "string", "enum": ["x64", "arm64"] },
                  "nodeVersion": {
                    "type": "string",
                    "description": "Node.js version to use for the executable.\n\nAccepts a valid semver string (e.g., `\"25.7.0\"`), or the special values `\"latest\"` / `\"latest-lts\"` which resolve the version automatically from  {@link  https://nodejs.org/dist/index.json } .\n\nThe minimum required version is 25.7.0, which is when SEA support was added to Node.js.",
                    "markdownDescription": "Node.js version to use for the executable.\n\nAccepts a valid semver string (e.g., `\"25.7.0\"`), or the special values\n`\"latest\"` / `\"latest-lts\"` which resolve the version automatically from\n {@link  https://nodejs.org/dist/index.json } .\n\nThe minimum required version is 25.7.0, which is when SEA support was added to Node.js."
                  }
                },
                "required": ["platform", "arch", "nodeVersion"],
                "additionalProperties": false
              },
              "description": "Cross-platform targets for building executables. Requires `@tsdown/exe` to be installed. When specified, builds an executable for each target platform/arch combination.",
              "markdownDescription": "Cross-platform targets for building executables.\nRequires `@tsdown/exe` to be installed.\nWhen specified, builds an executable for each target platform/arch combination."
            },
            "seaConfig": {
              "type": "object",
              "properties": {
                "executable": {
                  "type": "string",
                  "description": "Optional, if not specified, uses the current Node.js binary",
                  "markdownDescription": "Optional, if not specified, uses the current Node.js binary"
                },
                "disableExperimentalSEAWarning": {
                  "type": "boolean",
                  "default": true
                },
                "useSnapshot": { "type": "boolean", "default": false },
                "useCodeCache": { "type": "boolean", "default": false },
                "execArgv": { "type": "array", "items": { "type": "string" } },
                "execArgvExtension": {
                  "type": "string",
                  "enum": ["none", "env", "cli"],
                  "default": "env"
                },
                "assets": {
                  "type": "object",
                  "additionalProperties": { "type": "string" }
                }
              },
              "additionalProperties": false
            },
            "fileName": {
              "type": "string",
              "description": "Output file name without any suffix or extension. For example, do not include `.exe`, platform suffixes, or architecture suffixes.",
              "markdownDescription": "Output file name without any suffix or extension.\nFor example, do not include `.exe`, platform suffixes, or architecture suffixes."
            },
            "outDir": {
              "type": "string",
              "description": "Output directory for executables.",
              "markdownDescription": "Output directory for executables.",
              "default": "build"
            }
          }
        }
      ]
    },
    "Workspace": {
      "type": "object",
      "properties": {
        "include": {
          "anyOf": [
            { "type": "string", "const": "auto" },
            { "type": "string" },
            { "type": "array", "items": { "type": "string" } }
          ],
          "description": "Workspace directories. Glob patterns are supported.\n- `auto`: Automatically detect `package.json` files in the workspace.",
          "markdownDescription": "Workspace directories. Glob patterns are supported.\n- `auto`: Automatically detect `package.json` files in the workspace.",
          "default": "auto"
        },
        "exclude": {
          "$ref": "#/definitions/Arrayable%3Cstring%3E",
          "description": "Exclude directories from workspace. Defaults to all `node_modules`, `dist`, `test`, `tests`, `temp`, and `tmp` directories.",
          "markdownDescription": "Exclude directories from workspace.\nDefaults to all `node_modules`, `dist`, `test`, `tests`, `temp`, and `tmp` directories.",
          "default": [
            "**/node_modules/**",
            "**/dist/**",
            "**/test?(s)/**",
            "**/t?(e)mp/**"
          ]
        },
        "config": {
          "type": ["boolean", "string"],
          "description": "Path to the workspace configuration file.",
          "markdownDescription": "Path to the workspace configuration file."
        }
      },
      "additionalProperties": false
    }
  }
}
